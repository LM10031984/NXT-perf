import type { TrainingScenario } from "@/types/training";

/** Scénario "objections-acheteur" — non implémenté en Phase 6 (v2). */
export const objectionAcheteurScenario: TrainingScenario = (() => {
  throw new Error(
    "Scenario 'objections-acheteur' not implemented yet. " +
    "Implémentation prévue en Phase 7+ (voir ROADMAP.md v2 requirements)."
  );
})();
