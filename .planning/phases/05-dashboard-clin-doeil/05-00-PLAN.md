---
phase: 05-dashboard-clin-doeil
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/conseiller/dashboard/Top3PrioritesSection.tsx
  - src/lib/dashboard-priorities.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Les fichiers squelettes compilent sans erreur TypeScript"
    - "Les types PriorityCard et RatioToPriorityMap sont exportés et corrects"
    - "Le composant Top3PrioritesSection rend 3 placeholders sans crasher"
    - "La lib dashboard-priorities.ts exporte derivePriorityCards et RATIO_SITUATION_MAP"
  artifacts:
    - path: "src/components/conseiller/dashboard/Top3PrioritesSection.tsx"
      provides: "Squelette du composant — 3 cartes placeholder"
      min_lines: 40
    - path: "src/lib/dashboard-priorities.ts"
      provides: "Types PriorityCard + RATIO_SITUATION_MAP + stub derivePriorityCards"
      exports: ["PriorityCard", "RATIO_SITUATION_MAP", "derivePriorityCards"]
  key_links:
    - from: "src/lib/dashboard-priorities.ts"
      to: "src/types/ratios.ts"
      via: "import RatioId"
      pattern: "import.*RatioId"
    - from: "src/components/conseiller/dashboard/Top3PrioritesSection.tsx"
      to: "src/lib/dashboard-priorities.ts"
      via: "import PriorityCard, derivePriorityCards"
      pattern: "import.*dashboard-priorities"
---

<objective>
Wave 0 : créer les squelettes (types + composant stub) que les plans 05-01 et 05-02 implémenteront.
Cette vague garantit la continuité de compilation — pas de TypeScript rouge pendant l'implémentation.

Purpose: Définir les contrats de types avant l'implémentation (interface-first).
Output: `src/lib/dashboard-priorities.ts` + `src/components/conseiller/dashboard/Top3PrioritesSection.tsx` squelettes compilables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-clin-doeil/05-CONTEXT.md

@src/types/ratios.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Tâche 0-A : Créer src/lib/dashboard-priorities.ts (types + stubs)</name>
  <files>src/lib/dashboard-priorities.ts</files>
  <action>
Créer `src/lib/dashboard-priorities.ts` avec le contenu suivant (respecter l'ordre) :

1. **Type `PriorityCard`** (per D1, CONTEXT.md) :
```ts
import type { RatioId } from "@/types/ratios";
import type { SituationType } from "@/lib/constants";

export type PrioritySeverity = "rouge" | "orange" | "vert";

export type PriorityActionType = "training" | "copilot" | "saisie" | "view-ratio";

export interface PriorityAction {
  label: string;
  href: string;
  type: PriorityActionType;
}

export interface PriorityCard {
  severity: PrioritySeverity;
  ratioId: RatioId | string;       // string pour les volumes (VolumeKey)
  verdict: string;                  // 1 phrase FR
  metric: { value: number; target: number; unit: "%" | "" };
  action: PriorityAction;
}
```

2. **`RATIO_SITUATION_MAP`** (per D2, CONTEXT.md) — mapping `RatioId → SituationType | null` :
```ts
export const RATIO_SITUATION_MAP: Partial<Record<RatioId, SituationType>> = {
  contacts_rdv: "prospection" as SituationType,      // pas encore défini en SituationType — garder comme cast
  rdv_mandats: "estimation",
  pct_mandats_exclusifs: "mandats",
  acheteurs_visites: "objections-acheteur",
  visites_offre: "objections-acheteur",
  offres_compromis: "negociation-honoraires",
  compromis_actes: "follow-up",
};
```
Note : `contacts_rdv` n'a pas de situation directe dans SITUATION_PERSONA_MAP — le mapping peut retourner `"estimation"` comme fallback dans derivePriorityCards.

3. **`derivePriorityCards` stub** qui retourne 3 cartes placeholder (sera remplacé en plan 05-01) :
```ts
import type { ComputedRatio } from "@/types/ratios";

/** Stub Wave 0 — remplacé en plan 05-01 */
export function derivePriorityCards(
  _computedRatios: ComputedRatio[]
): [PriorityCard, PriorityCard, PriorityCard] {
  const placeholder: PriorityCard = {
    severity: "orange",
    ratioId: "rdv_mandats",
    verdict: "Chargement…",
    metric: { value: 0, target: 0, unit: "" },
    action: { label: "Voir le diagnostic", href: "/conseiller/diagnostic", type: "view-ratio" },
  };
  return [
    { ...placeholder, severity: "rouge" },
    { ...placeholder, severity: "orange" },
    { ...placeholder, severity: "vert", verdict: "Calcul en cours…" },
  ];
}
```

Garder les imports TypeScript stricts — pas de `// @ts-ignore`. Utiliser les vrais caractères FR (é, è, à).
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "dashboard-priorities" || echo "OK — aucune erreur dashboard-priorities"</automated>
  </verify>
  <done>Le fichier existe, exporte PriorityCard, RATIO_SITUATION_MAP, derivePriorityCards. `npx tsc --noEmit` ne produit aucune erreur sur ce fichier.</done>
</task>

<task type="auto">
  <name>Tâche 0-B : Créer src/components/conseiller/dashboard/Top3PrioritesSection.tsx (squelette)</name>
  <files>src/components/conseiller/dashboard/Top3PrioritesSection.tsx</files>
  <action>
Créer le dossier si nécessaire : `src/components/conseiller/dashboard/`.

Créer `Top3PrioritesSection.tsx` avec :

```tsx
"use client";

import { derivePriorityCards } from "@/lib/dashboard-priorities";
import type { PriorityCard } from "@/lib/dashboard-priorities";
import { useRatios } from "@/hooks/use-ratios";
import { cn } from "@/lib/utils";

/** Wave 0 stub — UI complète en plan 05-01 */

interface PriorityCardDisplayProps {
  card: PriorityCard;
}

function PriorityCardDisplay({ card }: PriorityCardDisplayProps) {
  const borderColor = {
    rouge: "border-red-500/40",
    orange: "border-orange-500/40",
    vert: "border-green-500/40",
  }[card.severity];

  return (
    <div
      className={cn(
        "flex flex-col gap-3 rounded-[var(--radius-card)] border bg-card/60 p-4",
        borderColor
      )}
    >
      <p className="text-sm text-muted-foreground">{card.verdict}</p>
      <a
        href={card.action.href}
        className="mt-auto inline-flex items-center justify-center rounded-[var(--radius-button)] bg-primary/10 px-3 py-1.5 text-xs font-medium text-primary transition-colors hover:bg-primary/20"
      >
        {card.action.label}
      </a>
    </div>
  );
}

export function Top3PrioritesSection() {
  const { computedRatios } = useRatios();
  const [card1, card2, card3] = derivePriorityCards(computedRatios);

  return (
    <section
      aria-label="Mes 3 priorités"
      className="mx-auto max-w-6xl px-4"
    >
      <h2 className="mb-3 text-sm font-semibold uppercase tracking-wider text-muted-foreground">
        Mes 3 priorités
      </h2>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-3">
        <PriorityCardDisplay card={card1} />
        <PriorityCardDisplay card={card2} />
        <PriorityCardDisplay card={card3} />
      </div>
    </section>
  );
}
```

Pas de `// @ts-ignore`. Caractères FR réels.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "Top3Priorites" || echo "OK — aucune erreur Top3PrioritesSection"</automated>
  </verify>
  <done>Le composant existe, importe depuis dashboard-priorities, rend 3 cartes. `npx tsc --noEmit` passe sans erreur sur ce fichier.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — 0 nouvelles erreurs introduites par les 2 fichiers créés.
</verification>

<success_criteria>
- `src/lib/dashboard-priorities.ts` existe avec les types PriorityCard, RATIO_SITUATION_MAP, derivePriorityCards
- `src/components/conseiller/dashboard/Top3PrioritesSection.tsx` existe et rend 3 cartes placeholder
- TypeScript strict passe sur les 2 fichiers
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-dashboard-clin-doeil/05-00-SUMMARY.md`
</output>
