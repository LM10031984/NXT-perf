---
phase: 05-dashboard-clin-doeil
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-00"
files_modified:
  - src/components/layout/header.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Le header affiche un bouton '?' qui relance le guided tour depuis l'étape 1"
    - "Cliquer '?' appelle resetTourStatus puis recharge la page pour déclencher le tour"
    - "Les boutons existants (notifications, thème, export, logout) restent fonctionnels"
    - "Le bouton HelpCircle existant dans le header est le même bouton amélioré — pas un doublon"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Bouton ? qui relance le tour guidé (D3 CONTEXT.md)"
      contains: "resetTourStatus"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/lib/guided-tour.ts"
      via: "resetTourStatus(tourRole) + getTourRole()"
      pattern: "resetTourStatus"
---

<objective>
Améliorer le bouton "?" dans le header pour qu'il relance le guided tour depuis l'étape 1 — per D3 CONTEXT.md.

Note : le header contient DÉJÀ un bouton HelpCircle (ligne 252-266 de header.tsx actuel). Ce plan le vérifie et l'améliore si nécessaire — il ne le recrée pas.

Purpose: Permettre à l'utilisateur de rejouer le guided tour à tout moment sans chercher dans les menus.
Output: Bouton "?" fonctionnel dans le header avec tooltip correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-clin-doeil/05-CONTEXT.md
@.planning/phases/05-dashboard-clin-doeil/05-00-SUMMARY.md

@src/components/layout/header.tsx
@src/lib/guided-tour.ts

<interfaces>
<!-- Contrats guided-tour.ts — extraits -->

```typescript
export function resetTourStatus(role: UserRole): void
export function getTourRole(availableRoles: UserRole[], mainRole: UserRole): UserRole
// resetTourStatus met status = "unseen" → au prochain reload, le tour se relance automatiquement
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 2-A : Vérifier et finaliser le bouton "?" dans header.tsx</name>
  <files>src/components/layout/header.tsx</files>
  <action>
**Lire header.tsx en entier avant toute modification.**

Le header contient déjà un bouton HelpCircle (chercher `HelpCircle` dans le fichier). Vérifier que :

1. Il utilise bien `resetTourStatus(tourRole)` puis `window.location.reload()` — per D3 CONTEXT.md
2. Il a le bon `title="Revoir le tour guidé"` (tooltip FR)
3. Il est visible pour TOUS les rôles (pas conditionné à manager/directeur)

**Si le bouton existe et est correct** (vérifier les 3 points) : aucune modification nécessaire — documenter dans le SUMMARY.

**Si le bouton manque ou est incorrect** : insérer ou corriger le bouton dans la zone des icônes du header, juste avant le bouton notifications (cloche), avec ce pattern :

```tsx
{/* Bouton relancer le tour guidé — D3 Phase 5 */}
<button
  type="button"
  onClick={() => {
    if (user) {
      const tourRole = getTourRole(user.availableRoles, user.mainRole);
      resetTourStatus(tourRole);
      window.location.reload();
    }
  }}
  title="Revoir le tour guidé"
  className="flex h-9 w-9 items-center justify-center rounded-[var(--radius-button)] text-muted-foreground transition-all duration-[var(--transition-fast)] hover:bg-muted hover:text-foreground"
>
  <HelpCircle className="h-4 w-4" />
</button>
```

Vérifier que `HelpCircle` est dans les imports Lucide en haut du fichier — si absent, l'ajouter.

**Contrainte absolue (DASH-06) :** NE PAS toucher aux autres boutons du header (thème, import, export, notifications, avatar, logout). Modification chirurgicale uniquement.

Pas de `// @ts-ignore`. Caractères FR réels.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && grep -n "resetTourStatus\|HelpCircle\|Revoir le tour" src/components/layout/header.tsx && npx tsc --noEmit 2>&1 | grep "header" || echo "OK — header sans erreur TS"</automated>
  </verify>
  <done>
- `grep resetTourStatus src/components/layout/header.tsx` retourne au moins 1 ligne
- `grep HelpCircle src/components/layout/header.tsx` retourne au moins 1 ligne (import + usage)
- `npx tsc --noEmit` ne produit pas d'erreur sur header.tsx
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf
npx tsc --noEmit
grep -c "resetTourStatus" src/components/layout/header.tsx
grep -c "HelpCircle" src/components/layout/header.tsx
```
Les deux greps doivent retourner >= 1. TypeScript passe.
</verification>

<success_criteria>
- Bouton "?" présent dans le header avec `title="Revoir le tour guidé"`
- onClick appelle `resetTourStatus` + `window.location.reload()`
- Tous les autres boutons du header inchangés (Bell, Sun/Moon, Upload, Download, LogOut, AvatarDisplay)
- `npx tsc --noEmit` passe
</success_criteria>

<output>
Après complétion, créer `.planning/phases/05-dashboard-clin-doeil/05-02-SUMMARY.md`
</output>
