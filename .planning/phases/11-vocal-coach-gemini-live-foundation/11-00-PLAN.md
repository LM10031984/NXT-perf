---
phase: 11-vocal-coach-gemini-live-foundation
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-gemini-live.ts
  - src/components/training/MicTestScreen.tsx
  - public/audio-worklets/pcm-processor.js
  - src/app/api/gemini/live-token/route.ts
  - src/app/(dashboard)/conseiller/training/_mic-test/page.tsx
autonomous: true
requirements:
  - VLIVE-01
  - VLIVE-02
  - VLIVE-03
  - VLIVE-04
  - VLIVE-05

must_haves:
  truths:
    - "Les fichiers squelettes existent et compilent sans erreur TypeScript"
    - "Le worklet audio est accessible comme asset statique depuis /audio-worklets/pcm-processor.js"
    - "Chaque fichier exporte les types et signatures conformes aux décisions D2-D6"
  artifacts:
    - path: "src/hooks/use-gemini-live.ts"
      provides: "Hook squelette avec interface UseGeminiLiveOptions + UseGeminiLiveResult exportés"
      exports: ["UseGeminiLiveOptions", "UseGeminiLiveResult", "useGeminiLive"]
    - path: "src/components/training/MicTestScreen.tsx"
      provides: "Composant squelette avec props MicTestScreenProps exportés"
      exports: ["MicTestScreen", "MicTestScreenProps"]
    - path: "public/audio-worklets/pcm-processor.js"
      provides: "AudioWorkletProcessor enregistré — peut être chargé via addModule()"
      contains: "registerProcessor"
    - path: "src/app/api/gemini/live-token/route.ts"
      provides: "Route POST stubée retournant 501 Not Implemented"
      exports: ["POST"]
    - path: "src/app/(dashboard)/conseiller/training/_mic-test/page.tsx"
      provides: "Page de test squelette montant MicTestScreen"
  key_links:
    - from: "src/app/(dashboard)/conseiller/training/_mic-test/page.tsx"
      to: "src/components/training/MicTestScreen.tsx"
      via: "import direct"
    - from: "src/hooks/use-gemini-live.ts"
      to: "public/audio-worklets/pcm-processor.js"
      via: "audioContext.audioWorklet.addModule('/audio-worklets/pcm-processor.js')"
---

<objective>
Poser les squelettes Wave 0 : types + signatures de tous les fichiers que les plans 11-01, 11-02 et 11-03 vont implémenter. Les squelettes garantissent que `npx tsc --noEmit` passe dès le départ et que les plans suivants peuvent être exécutés en parallèle sans conflit de types.

Purpose: Nyquist sampling — les contrats d'interface sont établis avant l'implémentation.
Output: 5 fichiers squelettes compilables, worklet statique enregistré, page de test accessible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-vocal-coach-gemini-live-foundation/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Types + squelettes hook et composant</name>
  <files>src/hooks/use-gemini-live.ts, src/components/training/MicTestScreen.tsx</files>
  <action>
Créer `src/hooks/use-gemini-live.ts` avec exactement l'interface décrite en D3 du CONTEXT.md (per D3) :

```typescript
"use client";

export type GeminiLiveVoice = "Puck" | "Charon" | "Kore" | "Fenrir" | "Aoede";
export type GeminiLiveStatus =
  | "idle"
  | "connecting"
  | "connected"
  | "speaking"
  | "listening"
  | "error";

export interface UseGeminiLiveOptions {
  systemPrompt?: string;
  voice?: GeminiLiveVoice;
  onTranscript?: (text: string) => void;
  onAudioFrame?: (frame: Float32Array) => void;
}

export interface UseGeminiLiveResult {
  status: GeminiLiveStatus;
  error: string | null;
  connect: () => Promise<void>;
  disconnect: () => void;
  sendAudioFrame: (frame: Float32Array) => void;
  sendText: (text: string) => void;
}

export function useGeminiLive(_options: UseGeminiLiveOptions = {}): UseGeminiLiveResult {
  // STUB — implémentation complète dans le plan 11-02
  return {
    status: "idle",
    error: null,
    connect: async () => { throw new Error("non implémenté") },
    disconnect: () => {},
    sendAudioFrame: () => {},
    sendText: () => {},
  };
}
```

Créer `src/components/training/MicTestScreen.tsx` avec l'interface de props (per D5, D6) :

```typescript
"use client";

export interface MicTestScreenProps {
  onConfirmed: () => void;
  onBack?: () => void;
}

export function MicTestScreen({ onConfirmed, onBack }: MicTestScreenProps) {
  // STUB — implémentation complète dans le plan 11-03
  return (
    <div className="flex flex-col items-center gap-4 p-8">
      <p className="text-sm text-muted-foreground">Test micro — en cours d&apos;implémentation</p>
      <button onClick={onConfirmed} className="rounded bg-primary px-4 py-2 text-sm text-primary-foreground">
        Continuer (stub)
      </button>
      {onBack && (
        <button onClick={onBack} className="text-sm text-muted-foreground underline">
          Retour
        </button>
      )}
    </div>
  );
}
```

Créer le répertoire `src/components/training/` s'il n'existe pas.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "use-gemini-live|MicTestScreen" | head -20 || echo "OK — aucune erreur sur ces fichiers"</automated>
  </verify>
  <done>Les deux fichiers existent, compilent sans erreur, exportent leurs types.</done>
</task>

<task type="auto">
  <name>Tâche 2 : AudioWorklet statique + stub route API</name>
  <files>public/audio-worklets/pcm-processor.js, src/app/api/gemini/live-token/route.ts</files>
  <action>
Créer le répertoire `public/audio-worklets/` puis `public/audio-worklets/pcm-processor.js` (per D4). Ce fichier doit être un AudioWorkletProcessor minimal valide — il sera enrichi dans le plan 11-02 :

```javascript
/**
 * pcm-processor.js — AudioWorklet pour la capture micro Gemini Live
 * Convertit le flux micro en frames Float32Array 16 kHz mono.
 * STUB Wave 0 — logique de resampling complète dans le plan 11-02.
 */
class PcmProcessor extends AudioWorkletProcessor {
  process(inputs, _outputs, _params) {
    const input = inputs[0];
    if (input && input[0]) {
      // Envoie les frames brutes au thread principal
      this.port.postMessage({ type: "audio", data: input[0] });
    }
    return true; // keep alive
  }
}

registerProcessor("pcm-processor", PcmProcessor);
```

Créer `src/app/api/gemini/live-token/route.ts` stub (per D2) :

```typescript
import { NextResponse } from "next/server";

/**
 * POST /api/gemini/live-token
 * Génère un ephemeral token Gemini Live pour le client.
 * Auth + rate-limit implémentés dans le plan 11-01.
 * STUB Wave 0 — retourne 501 Not Implemented.
 */
export async function POST() {
  return NextResponse.json(
    { error: "Not implemented — see plan 11-01" },
    { status: 501 }
  );
}
```

Créer les répertoires intermédiaires nécessaires (`src/app/api/gemini/live-token/`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "live-token" | head -10 || echo "OK — route compile"</automated>
  </verify>
  <done>Le worklet existe dans public/ (asset statique, pas en src/). La route retourne 501. `npx tsc --noEmit` propre.</done>
</task>

<task type="auto">
  <name>Tâche 3 : Page de test squelette + .env.local.example</name>
  <files>src/app/(dashboard)/conseiller/training/_mic-test/page.tsx</files>
  <action>
Créer `src/app/(dashboard)/conseiller/training/_mic-test/page.tsx` (le préfixe `_` exclut cette route du routing Next.js public — c'est une route de dev uniquement, par convention Next.js les dossiers préfixés par `_` dans le répertoire `app` ne sont PAS des routes ; utiliser plutôt le préfixe en `page.tsx` au niveau du dossier. NOTE : dans Next.js App Router, les dossiers `_` sont ignorés du routeur. Donc la page sera accessible via `/conseiller/training/_mic-test` seulement si le nom de dossier n'est pas préfixé `_`. Utiliser le nom `mic-test` à la place pour que la route soit navigable en local) :

Créer `src/app/(dashboard)/conseiller/training/mic-test/page.tsx` :

```typescript
"use client";

import { MicTestScreen } from "@/components/training/MicTestScreen";
import { useRouter } from "next/navigation";

export default function MicTestPage() {
  const router = useRouter();

  return (
    <div className="flex min-h-screen items-center justify-center bg-background">
      <div className="w-full max-w-md">
        <MicTestScreen
          onConfirmed={() => {
            // En Phase 12, naviguera vers le scenario
            alert("Micro validé — route scenario à câbler en Phase 12");
          }}
          onBack={() => router.push("/conseiller/dashboard")}
        />
      </div>
    </div>
  );
}
```

Ensuite, vérifier que `.env.local.example` contient la variable `GEMINI_LIVE_MODEL`. Si ce fichier existe, y ajouter la ligne suivante dans la section Gemini (per D1, VLIVE-04). Si la section n'existe pas encore, ajouter en fin de fichier :

```
# Gemini Live — modèle temps-réel (WebSocket full-duplex)
GEMINI_LIVE_MODEL=gemini-2.0-flash-live-001
```

Lire le fichier .env.local.example avant de l'éditer pour ne pas écraser les lignes existantes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "mic-test" | head -10 || echo "OK — page compile"</automated>
  </verify>
  <done>La page `/conseiller/training/mic-test` existe, monte `MicTestScreen`, compile. `GEMINI_LIVE_MODEL` est dans `.env.local.example`.</done>
</task>

</tasks>

<verification>
Après les 3 tâches, vérifier globalement :
- `npx tsc --noEmit` passe sans nouvelle erreur sur les 5 fichiers créés
- `ls public/audio-worklets/pcm-processor.js` confirme l'asset statique
- `grep -r "registerProcessor" public/audio-worklets/pcm-processor.js` retourne une ligne
- `grep "GEMINI_LIVE_MODEL" .env.local.example` retourne une ligne
</verification>

<success_criteria>
- `src/hooks/use-gemini-live.ts` exporte `UseGeminiLiveOptions`, `UseGeminiLiveResult`, `useGeminiLive`
- `src/components/training/MicTestScreen.tsx` exporte `MicTestScreen` et `MicTestScreenProps`
- `public/audio-worklets/pcm-processor.js` contient `registerProcessor("pcm-processor", ...)`
- `src/app/api/gemini/live-token/route.ts` exporte `POST` et retourne 501
- `src/app/(dashboard)/conseiller/training/mic-test/page.tsx` monte `MicTestScreen`
- `GEMINI_LIVE_MODEL` est documenté dans `.env.local.example`
- `npx tsc --noEmit` propre
</success_criteria>

<output>
Après completion, créer `.planning/phases/11-vocal-coach-gemini-live-foundation/11-00-SUMMARY.md`
</output>
