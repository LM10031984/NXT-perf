---
phase: 11-vocal-coach-gemini-live-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "11-00"
  - "11-01"
  - "11-02"
files_modified:
  - src/components/training/MicTestScreen.tsx
  - src/app/(dashboard)/conseiller/training/mic-test/page.tsx
autonomous: false
requirements:
  - VLIVE-03
  - VLIVE-05

must_haves:
  truths:
    - "MicTestScreen affiche un visualizer de niveau audio qui réagit en temps réel quand l'utilisateur parle"
    - "Si la permission micro est refusée, un écran FR clair s'affiche avec bouton Réessayer et bouton Retour — pas de crash"
    - "Le bouton Continuer appelle onConfirmed() et est disponible seulement après que l'utilisateur a validé que le micro fonctionne"
    - "La page /conseiller/training/mic-test est navigable et affiche MicTestScreen"
    - "npx tsc --noEmit passe propre sur tous les fichiers de la phase"
  artifacts:
    - path: "src/components/training/MicTestScreen.tsx"
      provides: "Composant complet : permission micro, visualizer niveau, fallback, bouton Continuer"
      exports: ["MicTestScreen", "MicTestScreenProps"]
    - path: "src/app/(dashboard)/conseiller/training/mic-test/page.tsx"
      provides: "Page de test navigable montant MicTestScreen"
  key_links:
    - from: "src/components/training/MicTestScreen.tsx"
      to: "navigator.mediaDevices.getUserMedia"
      via: "appel direct dans useEffect quand l'utilisateur clique sur Tester"
    - from: "src/components/training/MicTestScreen.tsx"
      to: "AudioContext + AnalyserNode"
      via: "visualizer de niveau (getByteFrequencyData ou getByteTimeDomainData)"
---

<objective>
Implémenter `MicTestScreen.tsx` : écran d'onboarding audio affiché avant chaque scenario Gemini Live. Demande la permission micro, affiche un visualizer de niveau, permet de tester ("Dire allô"), et gère le fallback si permission refusée. Finaliser la page de test et vérifier la compilation globale de la phase.

Purpose: L'utilisateur peut valider que son micro fonctionne avant d'entrer dans un scenario vocal (per D5, D6).
Output: Composant UI fonctionnel + page accessible + phase compilable de bout en bout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-vocal-coach-gemini-live-foundation/11-CONTEXT.md
@.planning/phases/11-vocal-coach-gemini-live-foundation/11-00-SUMMARY.md
@.planning/phases/11-vocal-coach-gemini-live-foundation/11-01-SUMMARY.md
@.planning/phases/11-vocal-coach-gemini-live-foundation/11-02-SUMMARY.md
@src/components/vocal/VocalFlow.tsx
@src/hooks/use-vocal-recorder.ts

<interfaces>
<!-- Contrats établis par le plan 11-00. Ne pas modifier les exports. -->

Depuis src/components/training/MicTestScreen.tsx (squelette 11-00) :
```typescript
export interface MicTestScreenProps {
  onConfirmed: () => void;
  onBack?: () => void;
}
```

Styles de référence (VocalFlow.tsx) :
- Animations CSS injectées via <style dangerouslySetInnerHTML>
- Boutons : `rounded-[var(--radius-button)] bg-primary text-primary-foreground`
- Cards : `rounded-lg border border-border bg-card`
- Icônes Lucide : `Mic`, `MicOff`, `AlertTriangle`, `CheckCircle2`, `RefreshCw`, `ArrowLeft`
- Textes : toujours en français avec vrais caractères (é, è, à, ç)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : MicTestScreen — permission + visualizer + fallback</name>
  <files>src/components/training/MicTestScreen.tsx</files>
  <action>
Remplacer le stub Wave 0 par l'implémentation complète. Le composant a 4 états internes :

**État 1 : "idle"** (initial)
- Centré, icône micro, titre "Test du microphone"
- Description courte (2 lignes max) : "Avant de commencer, vérifions que ton micro fonctionne correctement."
- Bouton principal "Tester le microphone" → déclenche `getUserMedia`

**État 2 : "testing"**
- Afficher un visualizer de niveau audio : un `<canvas>` ou une rangée de barres CSS animées reflétant le niveau RMS du flux micro
- Utiliser `AudioContext` + `AnalyserNode` + `requestAnimationFrame` pour lire `getByteFrequencyData` en temps réel
- Afficher le texte "Parle maintenant pour voir ton micro réagir"
- Deux boutons en bas : "Recommencer" (relance getUserMedia) et "Le micro fonctionne — Continuer" → appelle `onConfirmed()`
- NOTE : le bouton "Continuer" est disponible immédiatement une fois l'état "testing" atteint (la validation visuelle suffit, pas besoin d'un seuil de niveau minimum)

**État 3 : "error" (permission refusée ou erreur getUserMedia)**
- Icône `AlertTriangle` amber
- Titre "Permission micro refusée" (per D6)
- Texte : "Active-la dans tes paramètres navigateur pour utiliser le training vocal."
- Bouton "Réessayer" → retourne à l'état idle et re-tente getUserMedia
- Si `onBack` est défini : bouton secondaire "Retour" → appelle `onBack()`
- PAS de crash silencieux — toujours afficher cet écran en cas d'erreur

**État 4 : "confirmed"** (non utilisé dans ce composant — `onConfirmed()` emmène vers le scenario)

Implémentation du visualizer (simple, sans librairie) :
- Créer `AudioContext`, `AnalyserNode` (fftSize 256), connecter `MediaStreamSource → Analyser`
- `requestAnimationFrame` loop qui lit `getByteFrequencyData` et calcule le niveau moyen
- Afficher via des barres CSS (10-16 barres) dont la hauteur est proportionnelle au niveau
- Nettoyer dans `useEffect` cleanup : `audioContext.close()`, `stream.getTracks().forEach(t => t.stop())`

Contraintes :
- `"use client"` en en-tête
- Aucun `// @ts-ignore`
- Textes en français avec vrais caractères
- Tailwind CSS 4 uniquement pour le style (pas de CSS-in-JS sauf les animations déjà présentes dans VocalFlow.tsx pour cohérence)
- Lucide icons : `Mic`, `MicOff`, `AlertTriangle`, `CheckCircle2`, `RefreshCw`, `ArrowLeft`

Structure du fichier :
```typescript
"use client";

import { useState, useEffect, useRef, useCallback } from "react";
import { Mic, MicOff, AlertTriangle, CheckCircle2, RefreshCw, ArrowLeft } from "lucide-react";
import { cn } from "@/lib/utils";

export interface MicTestScreenProps {
  onConfirmed: () => void;
  onBack?: () => void;
}

type MicTestState = "idle" | "testing" | "error";

export function MicTestScreen({ onConfirmed, onBack }: MicTestScreenProps) {
  const [state, setState] = useState<MicTestState>("idle");
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [levels, setLevels] = useState<number[]>(Array(14).fill(0));

  const audioContextRef = useRef<AudioContext | null>(null);
  const streamRef = useRef<MediaStream | null>(null);
  const animFrameRef = useRef<number | null>(null);
  const analyserRef = useRef<AnalyserNode | null>(null);

  // Cleanup complet
  const cleanup = useCallback(() => { ... }, []);

  // Démarrer le test micro
  const startTest = useCallback(async () => { ... }, [cleanup]);

  // Cleanup on unmount
  useEffect(() => () => { cleanup(); }, [cleanup]);

  if (state === "idle") return <IdleScreen onStart={startTest} />;
  if (state === "error") return <ErrorScreen message={errorMessage} onRetry={startTest} onBack={onBack} />;
  return <TestingScreen levels={levels} onContinue={onConfirmed} onRetry={startTest} />;
}

// Sub-composants : IdleScreen, ErrorScreen, TestingScreen
// (dans le même fichier pour la cohérence avec VocalFlow.tsx)
```

Implémenter tous les sous-composants dans le même fichier.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "MicTestScreen" | head -20 || echo "OK — composant compile sans erreur"</automated>
  </verify>
  <done>
- Composant compile, exporte `MicTestScreen` et `MicTestScreenProps`
- 3 états visuels distincts : idle, testing, error
- Visualizer utilise `AnalyserNode` (pas de boucle setInterval — requestAnimationFrame)
- Cleanup audio dans useEffect
- Textes FR avec vrais caractères
- Aucun `// @ts-ignore`
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Page de test finale + vérification compilation globale</name>
  <files>src/app/(dashboard)/conseiller/training/mic-test/page.tsx</files>
  <action>
Mettre à jour `src/app/(dashboard)/conseiller/training/mic-test/page.tsx` pour monter le `MicTestScreen` réel (plus le stub), avec un header clair indiquant que c'est une page de validation de développement.

```typescript
"use client";

import { MicTestScreen } from "@/components/training/MicTestScreen";
import { useRouter } from "next/navigation";

/**
 * Page de test développeur : /conseiller/training/mic-test
 * Permet à Laurent de valider que le micro capture et que l'UI MicTestScreen fonctionne.
 * En Phase 12, MicTestScreen sera monté dans VocalCoachScreen avant chaque scenario.
 */
export default function MicTestPage() {
  const router = useRouter();

  return (
    <div className="flex min-h-screen items-center justify-center bg-background p-4">
      <div className="w-full max-w-md">
        <div className="mb-6 rounded-lg border border-amber-500/20 bg-amber-500/5 px-4 py-3">
          <p className="text-xs font-medium text-amber-600">
            Page de validation développement — Phase 11
          </p>
          <p className="mt-0.5 text-xs text-muted-foreground">
            Valide que le micro capture avant de câbler les scenarios Gemini Live en Phase 12.
          </p>
        </div>
        <MicTestScreen
          onConfirmed={() => {
            alert("Micro validé. En Phase 12, la navigation vers le scenario sera câblée ici.");
          }}
          onBack={() => router.push("/conseiller/dashboard")}
        />
      </div>
    </div>
  );
}
```

Ensuite, vérifier la compilation globale de toute la phase :
- `npx tsc --noEmit` doit passer propre
- `npx next lint` ou `npx eslint src/hooks/use-gemini-live.ts src/components/training/MicTestScreen.tsx src/app/api/gemini/live-token/route.ts --max-warnings 0` doit passer (ou signaler seulement des warnings tolérables — corriger les erreurs lint)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
- `npx tsc --noEmit` retourne exit 0 (ou "Found 0 errors")
- La page `/conseiller/training/mic-test` compile
- Aucun fichier créé dans cette phase ne contient `// @ts-ignore`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Route POST /api/gemini/live-token (auth + rate-limit + demo mode)
    - AudioWorklet public/audio-worklets/pcm-processor.js (resampling 16 kHz)
    - Hook use-gemini-live.ts (WebSocket + AudioWorklet state machine)
    - Composant MicTestScreen.tsx (permission micro + visualizer + fallback)
    - Page /conseiller/training/mic-test (navigable en dev)
  </what-built>
  <how-to-verify>
1. Démarrer le serveur dev : `npx next dev --port 3000 --hostname 0.0.0.0`

2. Test compilation :
   - `npx tsc --noEmit` → doit retourner 0 erreur

3. Test route demo mode :
   - Ouvrir le terminal, lancer :
     `curl -X POST http://localhost:3000/api/gemini/live-token -H "X-Demo-Mode: true"`
   - Résultat attendu : `{"token":"demo-live-token-...","expiresAt":"...","demo":true}`

4. Test page MicTestScreen :
   - Naviguer vers http://localhost:3000/conseiller/training/mic-test
   - Vérifier que l'écran initial "Test du microphone" s'affiche
   - Cliquer "Tester le microphone" → accepter la permission dans le navigateur
   - Vérifier que les barres du visualizer bougent quand tu parles
   - Vérifier le bouton "Le micro fonctionne — Continuer" → l'alert "Micro validé" s'affiche

5. Test fallback permission refusée :
   - Recharger la page
   - Cliquer "Tester le microphone" → refuser la permission navigateur
   - Vérifier que l'écran "Permission micro refusée" s'affiche avec boutons "Réessayer" et "Retour"
   - Cliquer "Retour" → navigue vers /conseiller/dashboard

6. Test sans `// @ts-ignore` :
   - `grep -r "ts-ignore" src/hooks/use-gemini-live.ts src/components/training/MicTestScreen.tsx src/app/api/gemini/live-token/route.ts`
   - Résultat attendu : aucune ligne
  </how-to-verify>
  <resume-signal>Taper "approuvé" si tout fonctionne, ou décrire les problèmes constatés pour correction.</resume-signal>
</task>

</tasks>

<verification>
Vérification finale de phase :
- `npx tsc --noEmit` → 0 erreur
- `grep -r "ts-ignore" src/hooks/use-gemini-live.ts src/components/training/MicTestScreen.tsx src/app/api/gemini/live-token/route.ts` → vide
- `grep "GEMINI_LIVE_MODEL" .env.local.example` → présent
- `ls public/audio-worklets/pcm-processor.js` → fichier présent
- Checkpoint human-verify approuvé
</verification>

<success_criteria>
1. `src/hooks/use-gemini-live.ts` implémenté avec WebSocket + AudioWorklet state machine
2. `public/audio-worklets/pcm-processor.js` resamplé 16 kHz, chargé sans erreur
3. `src/components/training/MicTestScreen.tsx` affiche permission/visualizer/fallback FR
4. `src/app/api/gemini/live-token/route.ts` retourne token (auth + rate-limit + demo)
5. Page `/conseiller/training/mic-test` navigable et fonctionnelle
6. `GEMINI_LIVE_MODEL` dans `.env.local.example`
7. `npx tsc --noEmit` propre sur tous les fichiers de la phase
8. Permission micro refusée → écran fallback FR, pas de crash silencieux
9. Zéro `// @ts-ignore` dans les fichiers produits
</success_criteria>

<output>
Après completion et validation du checkpoint, créer `.planning/phases/11-vocal-coach-gemini-live-foundation/11-03-SUMMARY.md`
</output>
