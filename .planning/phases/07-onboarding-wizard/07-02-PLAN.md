---
phase: 07-onboarding-wizard
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-00"
  - "07-01"
files_modified:
  - src/components/onboarding/conseiller-registration-wizard/steps/StepRole.tsx
  - src/components/onboarding/conseiller-registration-wizard/steps/StepOrgChoice.tsx
  - src/components/onboarding/conseiller-registration-wizard/steps/StepDetails.tsx
  - src/components/onboarding/conseiller-registration-wizard/steps/StepConfirmation.tsx
  - src/components/onboarding/conseiller-registration-wizard/ConseillerRegistrationWizard.tsx
  - src/app/(auth)/register/page.tsx
  - e2e/register-non-conseiller.spec.ts
autonomous: true
requirements:
  - ONBO-01
  - ONBO-02
  - ONBO-04

must_haves:
  truths:
    - "L'inscription conseiller passe par le wizard 4 étapes (Rôle → Org → Détails → Confirmation)"
    - "Le wizard affiche 'Étape N sur 4' et 4 points à chaque étape"
    - "Chaque étape a un bouton Retour fonctionnel, y compris l'étape 1 qui renvoie vers /login"
    - "Les flows manager/directeur/coach/reseau dans register/page.tsx sont byte-identiques après ce plan"
    - "npx tsc --noEmit et npx playwright test e2e/register-non-conseiller.spec.ts passent tous les deux"
  artifacts:
    - path: "src/components/onboarding/conseiller-registration-wizard/steps/StepRole.tsx"
      provides: "Étape 1 — affiche 'Je suis agent immobilier', redirige autres rôles vers legacy form"
      exports: ["StepRole"]
    - path: "src/components/onboarding/conseiller-registration-wizard/steps/StepOrgChoice.tsx"
      provides: "Étape 2 — join vs create choice avec UI complète"
      exports: ["StepOrgChoice"]
    - path: "src/components/onboarding/conseiller-registration-wizard/steps/StepDetails.tsx"
      provides: "Étape 3 — prénom, nom, email, password, inviteCode ou agencyName selon orgChoice"
      exports: ["StepDetails"]
    - path: "src/components/onboarding/conseiller-registration-wizard/steps/StepConfirmation.tsx"
      provides: "Étape 4 — submit Supabase + success state + CTA 'Voir le dashboard'"
      exports: ["StepConfirmation"]
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Montage conditionnel du wizard pour role=conseiller, legacy form pour autres rôles"
  key_links:
    - from: "src/app/(auth)/register/page.tsx"
      to: "src/components/onboarding/conseiller-registration-wizard/ConseillerRegistrationWizard.tsx"
      via: "conditional render when isConseillerWizardMode"
      pattern: "ConseillerRegistrationWizard|isConseillerWizardMode"
    - from: "src/components/onboarding/conseiller-registration-wizard/steps/StepConfirmation.tsx"
      to: "supabase.auth.signUp"
      via: "handleSubmit → createClient().auth.signUp"
      pattern: "supabase.*signUp|signUp.*supabase"
---

<objective>
Implémente les 4 étapes du wizard conseiller et monte le composant conditionnellement dans register/page.tsx. Les flows non-conseiller ne sont PAS modifiés — seule la condition de routage est ajoutée.

Purpose: ONBO-01, ONBO-02, ONBO-04 — livrer le wizard fonctionnel sans casser les flows existants.
Output: ConseillerRegistrationWizard complet, montage conditionnel dans register page, E2E wizard passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-onboarding-wizard/07-CONTEXT.md
@.planning/phases/07-onboarding-wizard/07-00-SUMMARY.md
@.planning/phases/07-onboarding-wizard/07-01-SUMMARY.md

@src/app/(auth)/register/page.tsx
@src/stores/onboarding-wizard-store.ts
@src/components/onboarding/conseiller-registration-wizard/ConseillerRegistrationWizard.tsx
</context>

<interfaces>
<!-- Contrats à respecter — extraits de register/page.tsx et du store -->

Depuis src/stores/onboarding-wizard-store.ts (créé en plan 07-00) :
```typescript
export type WizardStep = "role" | "org_choice" | "details" | "confirmation";
export type OrgChoice = "join" | "create" | null;
export interface WizardFormData {
  firstName: string; lastName: string; email: string;
  password: string; inviteCode: string; agencyName: string;
}
export const useOnboardingWizardStore: // Zustand store avec goToStep, goBack, setOrgChoice, updateFormData
```

Depuis src/app/(auth)/register/page.tsx — logique de submit à réutiliser dans StepConfirmation :
```typescript
// Même logique que handleSubmit() existant, adaptée pour le wizard :
// 1. createClient() → supabase.auth.signUp({ email, password, options: { data: { ... } } })
// 2. useAppStore.getState().setProfile(optimisticProfile)
// 3. supabase.from("subscriptions").upsert(...)
// 4. Effacer les cookies démo
// 5. setTimeout(1000) → window.location.href = "/onboarding/identite"
// NE PAS créer de nouvelle route API — même logique client-side que l'existant

// Type importé nécessaire :
import type { DbProfile } from "@/types/database";
import { useAppStore } from "@/stores/app-store";
```

Depuis src/app/(auth)/register/page.tsx — condition de montage à ajouter :
```typescript
// UNIQUEMENT ajouter cette logique AVANT le return de RegisterForm() :
// Détecter si on est en mode wizard conseiller :
// const isConseillerWizardMode = !roleLocked && (
//   derivedProfile === "AGENT" ||
//   (!derivedProfile && !initialCode)  // visite directe sans code ni profile
// );
// if (isConseillerWizardMode) return <ConseillerRegistrationWizard />;
// Le reste du JSX existant est INCHANGÉ
```

Pattern InviteCodeFeedback (réutiliser depuis register/page.tsx — pas dupliquer) :
```typescript
// StepDetails peut importer InviteCodeFeedback depuis le même fichier si besoin
// OU recréer l'inline feedback — mais NE PAS toucher register/page.tsx pour l'exporter
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implémenter les 4 étapes du wizard</name>
  <files>
    src/components/onboarding/conseiller-registration-wizard/steps/StepRole.tsx,
    src/components/onboarding/conseiller-registration-wizard/steps/StepOrgChoice.tsx,
    src/components/onboarding/conseiller-registration-wizard/steps/StepDetails.tsx,
    src/components/onboarding/conseiller-registration-wizard/steps/StepConfirmation.tsx
  </files>
  <behavior>
    - StepRole : clic "Je suis agent immobilier" → goToStep("org_choice")
    - StepRole : bouton "Retour" → router.push("/login")
    - StepOrgChoice : clic "Rejoindre" → setOrgChoice("join"), goToStep("details")
    - StepOrgChoice : clic "Créer mon agence" → setOrgChoice("create"), goToStep("details")
    - StepOrgChoice : bouton "Retour" → goBack()
    - StepDetails : orgChoice==="join" → affiche champ inviteCode, pas agencyName
    - StepDetails : orgChoice==="create" → affiche champ agencyName, pas inviteCode
    - StepDetails : tous les champs requis (firstName, lastName, email, password) + conditionnel
    - StepDetails : validation inline (champs vides → message d'erreur, PAS de submit)
    - StepDetails : bouton "Continuer" → goToStep("confirmation") si valide
    - StepConfirmation : résumé de saisie + bouton "Créer mon compte" → handleSubmit()
    - StepConfirmation : handleSubmit() appelle supabase.auth.signUp avec même payload que register/page.tsx
    - StepConfirmation : succès → window.location.href = "/onboarding/identite"
    - StepConfirmation : erreur → afficher message inline
    - WizardProgressIndicator : affiche "Étape N sur 4" + 4 dots (dot actif = ring)
  </behavior>
  <read_first>
    Lire src/app/(auth)/register/page.tsx entier pour extraire la logique handleSubmit() exacte (lignes 118-251).
    Lire src/stores/onboarding-wizard-store.ts pour connaître l'API du store.
    Lire src/components/onboarding/conseiller-registration-wizard/steps/StepRole.tsx (squelette de plan 00) pour ne pas repartir de zéro.
  </read_first>
  <action>
    Remplacer chaque step squelette par l'implémentation complète.

    CONTRAINTES CRITIQUES :
    1. Pas de `// @ts-ignore`
    2. Pas de nouvelles dépendances npm
    3. Utiliser les mêmes className Tailwind que register/page.tsx pour la cohérence visuelle
       (h-10 rounded-lg border border-input bg-background px-3 text-sm, etc.)
    4. StepConfirmation doit réutiliser la MÊME logique signUp que register/page.tsx — copier la logique, pas créer d'API
    5. Les data-testid doivent être conservés et complétés :
       - StepRole : data-testid="wizard-role-agent-btn", data-testid="wizard-back-to-login"
       - StepOrgChoice : data-testid="wizard-org-join-btn", data-testid="wizard-org-create-btn"
       - StepDetails : data-testid="wizard-details-next-btn", data-testid="wizard-details-back"
       - StepConfirmation : data-testid="wizard-submit-btn", data-testid="wizard-confirmation-dashboard-btn"
    6. Le WizardProgressIndicator est déjà implémenté (plan 00) — ne pas le modifier
    7. "Retour" sur l'étape 1 (StepRole) → router.push("/login") (per D5 dans 07-CONTEXT.md)

    LOGIQUE SUPABASE dans StepConfirmation (copier depuis register/page.tsx handleSubmit) :
    - Récupérer formData depuis useOnboardingWizardStore()
    - main_role = "conseiller"
    - selected_roles = ["conseiller"]
    - category = "confirme" (default — pas de sélection dans le wizard, per D2)
    - context_mode = formData.inviteCode ? "invite" : "personal"
    - Mêmes champs optimisticProfile que register/page.tsx
    - Mêmes cookies démo à effacer
    - window.location.href = "/onboarding/identite" après 1s
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "StepRole|StepOrgChoice|StepDetails|StepConfirmation" || echo "NO_TSC_ERRORS"</automated>
  </verify>
  <done>
    Les 4 steps sont implémentés et compilent TypeScript.
    Les data-testid sont présents sur les boutons clés.
    La logique de back button est fonctionnelle à chaque étape.
    StepConfirmation contient la logique supabase.auth.signUp complète.
  </done>
</task>

<task type="auto">
  <name>Task 2: Montage conditionnel dans register/page.tsx + E2E wizard</name>
  <files>
    src/app/(auth)/register/page.tsx,
    e2e/register-non-conseiller.spec.ts
  </files>
  <read_first>
    Lire src/app/(auth)/register/page.tsx pour identifier l'emplacement précis où insérer la condition.
    Le point d'insertion est à l'intérieur de RegisterForm(), APRÈS la déclaration de toutes les variables d'état (useState hooks), AVANT le return JSX.
    Lire e2e/register-non-conseiller.spec.ts pour la structure des tests wizard à implémenter.
  </read_first>
  <action>
    MODIFICATION DE register/page.tsx :

    1. Ajouter l'import en haut du fichier (après les imports existants) :
       ```typescript
       import { ConseillerRegistrationWizard } from "@/components/onboarding/conseiller-registration-wizard";
       ```

    2. À l'intérieur de RegisterForm(), après TOUS les hooks useState/useCallback/useEffect existants
       (c'est-à-dire juste avant `const inputClassName = ...`), ajouter :
       ```typescript
       // Wizard conseiller — mount conditionnel (per D1, D2 dans 07-CONTEXT.md)
       // Critères : pas de code d'invitation dans l'URL ET profil AGENT ou visite directe
       const isConseillerWizardMode =
         !roleLocked &&
         (derivedProfile === "AGENT" || (!derivedProfile && !profileParam && !legacyRole));
       ```

    3. Juste après `const inputClassName = ...`, ajouter :
       ```typescript
       if (isConseillerWizardMode) {
         return <ConseillerRegistrationWizard />;
       }
       ```

    4. AUCUNE AUTRE MODIFICATION. Toutes les lignes suivantes du fichier sont identiques.

    VÉRIFICATION CRITIQUE : après modification, vérifier que le code des flows manager/directeur/coach/reseau est byte-identique. Ces flows s'activent quand `roleLocked === true` (code dans l'URL) ou quand `derivedProfile !== "AGENT"` et `legacyRole` est présent. La condition `isConseillerWizardMode` ne s'active pas dans ces cas.

    MODIFICATION DE e2e/register-non-conseiller.spec.ts :

    Implémenter les 5 tests de la section "Wizard conseiller — happy path" :

    Test 1 "Étape 1 : affiche 'Quel est votre rôle ?'" :
      ```typescript
      await page.goto("/register");
      await expect(page.getByText("Quel est votre rôle ?")).toBeVisible({ timeout: 10_000 });
      await expect(page.getByText("Étape 1 sur 4")).toBeVisible();
      ```

    Test 2 "Étape 2 : affiche les options org après clic 'Je suis agent immobilier'" :
      ```typescript
      await page.goto("/register");
      await page.getByTestId("wizard-role-agent-btn").click();
      await expect(page.getByText("Étape 2 sur 4")).toBeVisible({ timeout: 5_000 });
      await expect(page.getByTestId("wizard-org-join-btn")).toBeVisible();
      await expect(page.getByTestId("wizard-org-create-btn")).toBeVisible();
      ```

    Test 3 "L'indicateur de progression affiche 'Étape N sur 4'" :
      ```typescript
      await page.goto("/register");
      await expect(page.getByText("Étape 1 sur 4")).toBeVisible({ timeout: 10_000 });
      await page.getByTestId("wizard-role-agent-btn").click();
      await expect(page.getByText("Étape 2 sur 4")).toBeVisible({ timeout: 5_000 });
      ```

    Test 4 "Le bouton Retour à l'étape 2 revient à l'étape 1" :
      ```typescript
      await page.goto("/register");
      await page.getByTestId("wizard-role-agent-btn").click();
      await expect(page.getByText("Étape 2 sur 4")).toBeVisible({ timeout: 5_000 });
      await page.getByTestId("wizard-details-back").click(); // ou bouton "Retour" à l'étape 2
      // Utiliser le bon testid selon l'étape — StepOrgChoice n'a pas wizard-details-back
      // Utiliser plutôt : page.getByRole("button", { name: /Retour/i }).click()
      await expect(page.getByText("Étape 1 sur 4")).toBeVisible({ timeout: 5_000 });
      ```

    Test 5 "Le bouton Retour à l'étape 1 pointe vers /login" :
      ```typescript
      await page.goto("/register");
      await expect(page.getByText("Étape 1 sur 4")).toBeVisible({ timeout: 10_000 });
      await page.getByRole("button", { name: /Retour/i }).click();
      await expect(page).toHaveURL(/\/login/, { timeout: 5_000 });
      ```

    SUPPRIMER les test.skip() sur ces 5 tests uniquement.
    Les tests smoke + manager + directeur + conseiller legacy restent comme en plan 07-01.

    IMPORTANT :
    - Pas de `// @ts-ignore`
    - Adapter les sélecteurs si les data-testid définis en Task 1 diffèrent de "wizard-details-back"
      (utiliser le testid réel défini dans StepOrgChoice.tsx — par défaut, ajouter data-testid="wizard-org-back" sur le bouton Retour de StepOrgChoice)
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx playwright test e2e/register-non-conseiller.spec.ts --reporter=line 2>&1 | tail -30</automated>
  </verify>
  <done>
    `npx playwright test e2e/register-non-conseiller.spec.ts` : TOUS les tests passing (0 failures, 0 skipped).
    `npx tsc --noEmit --skipLibCheck` : 0 nouvelles erreurs.
    `git diff src/app/(auth)/register/page.tsx` : seulement 3 lignes ajoutées (import + const + if block).
    L'URL `/register?profile=MANAGER` (ou `?role=manager`) n'affiche PAS le wizard (legacy form).
    L'URL `/register` sans paramètre affiche le wizard.
    L'URL `/register?code=AG-1234` (roleLocked=true) n'affiche PAS le wizard (legacy form).
  </done>
</task>

</tasks>

<acceptance_criteria>
```bash
# 1. TypeScript sans erreur
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit --skipLibCheck 2>&1 | grep -v "^$" | wc -l
# Doit retourner 0 (ou le même nombre qu'avant ce plan)

# 2. Tous les E2E passent (smoke + non-conseiller + wizard)
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx playwright test e2e/register-non-conseiller.spec.ts --reporter=line 2>&1 | grep -E "passed|failed"
# Doit afficher "X passed" et "0 failed"

# 3. register/page.tsx modifié minimalement (≤5 lignes ajoutées)
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && git diff --stat src/app/\(auth\)/register/page.tsx
# Insertions ≤ 5, suppressions = 0

# 4. Le wizard se monte sur /register sans paramètre
grep -n "ConseillerRegistrationWizard\|isConseillerWizardMode" /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/app/\(auth\)/register/page.tsx

# 5. Le wizard n'est PAS monté quand roleLocked=true (code dans URL)
grep -n "roleLocked" /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/app/\(auth\)/register/page.tsx
# roleLocked doit apparaître dans la condition isConseillerWizardMode

# 6. Progression indicator affiche le bon texte
grep -n "Étape.*sur 4" /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/components/onboarding/conseiller-registration-wizard/WizardProgressIndicator.tsx

# 7. Pas de @ts-ignore
grep -rn "@ts-ignore" /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/components/onboarding/conseiller-registration-wizard/
# Doit retourner 0 résultats

# 8. app-store.ts pas modifié
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && git diff src/stores/app-store.ts | wc -l
# Doit retourner 0
```
</acceptance_criteria>

<verification>
1. `npx tsc --noEmit --skipLibCheck` : 0 erreurs
2. `npx playwright test e2e/register-non-conseiller.spec.ts` : tous passed, 0 failed
3. `git diff src/app/(auth)/register/page.tsx` : 3-5 lignes ajoutées max (import + isConseillerWizardMode + if block)
4. `git diff src/stores/app-store.ts` : vide
5. Visite manuelle de `/register` → wizard étape 1 visible
6. Visite manuelle de `/register?profile=MANAGER` → legacy form visible
7. Visite manuelle de `/register?code=AG-1234` → legacy form (roleLocked) visible
</verification>

<success_criteria>
- ConseillerRegistrationWizard 4 étapes fonctionnel avec progress indicator et bouton Retour à chaque étape
- Montage conditionnel dans register/page.tsx : ≤5 lignes modifiées
- Flows manager/directeur/coach/reseau byte-identiques (0 suppressions dans git diff)
- 12+ tests E2E passing (7 non-conseiller legacy + 5 wizard)
- npx tsc --noEmit --skipLibCheck : 0 nouvelles erreurs
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-onboarding-wizard/07-02-SUMMARY.md`
</output>
