---
phase: 07-onboarding-wizard
plan: "01"
type: execute
wave: 1
depends_on:
  - "07-00"
files_modified:
  - e2e/register-non-conseiller.spec.ts
autonomous: true
requirements:
  - ONBO-03

must_haves:
  truths:
    - "Les tests E2E non-conseiller passent sur le code ACTUEL avant tout refactoring"
    - "L'UI de /register pour les rôles manager et directeur est couverte par au moins 3 tests passing"
    - "Aucune modification de src/app/(auth)/register/page.tsx dans ce plan"
  artifacts:
    - path: "e2e/register-non-conseiller.spec.ts"
      provides: "Tests E2E complets et passing pour les flows non-conseiller"
      min_lines: 80
  key_links:
    - from: "e2e/register-non-conseiller.spec.ts"
      to: "src/app/(auth)/register/page.tsx"
      via: "page.goto('/register')"
      pattern: "goto.*register"
---

<objective>
Implémente les tests E2E pour les parcours d'inscription non-conseiller. Ces tests DOIVENT passer sur le code actuel (avant le wizard). Ils constituent le filet de sécurité qui détectera toute régression introduite par le plan 07-02.

Purpose: PITFALLS I-6 — écrire les tests AVANT le refactoring, pas après. Si un test échoue sur ce plan, le wizard ne doit pas être shipé.
Output: e2e/register-non-conseiller.spec.ts complet et passing sur le code inchangé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-onboarding-wizard/07-CONTEXT.md
@.planning/phases/07-onboarding-wizard/07-00-SUMMARY.md

@src/app/(auth)/register/page.tsx
</context>

<interfaces>
<!-- Contrats UI extraits de register/page.tsx — ce que les tests doivent cibler -->

Depuis src/app/(auth)/register/page.tsx (comportements actuels à tester) :

```typescript
// Titre principal
<h1>Créer un compte</h1>

// Boutons de rôle (grid 2 cols)
// "Conseiller", "Manager", "Directeur" (3 boutons, pas "reseau" ni "coach" dans l'UI publique)
// onClick={() => toggleRole(r)}

// Quand manager/directeur sélectionné : isManager = true
// → Affiche bloc "Organisation" avec tab "Créer une organisation" / "Rejoindre avec un code"

// Quand conseiller only : isConseillerOnly = true
// → Affiche champ "Code d'invitation (optionnel)"

// InviteCodeFeedback :
// status "idle" → rien
// status "checking" → spinner + "Vérification..."
// status "valid" → "Code valide — Organisation : {orgName}"
// status "invalid" → "Code invalide"
```

Pattern test existant (depuis e2e/accounts-roles.spec.ts) :
```typescript
test("Register page loads", async ({ page }) => {
  await page.goto("/register");
  await expect(page.getByText("Créer un compte")).toBeVisible({ timeout: 10_000 });
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implémenter les tests smoke + manager + directeur</name>
  <files>e2e/register-non-conseiller.spec.ts</files>
  <read_first>
    Lire e2e/register-non-conseiller.spec.ts (créé en plan 07-00) pour connaître la structure exacte des describe/test existants.
    Lire src/app/(auth)/register/page.tsx entier (577 lignes) pour identifier les sélecteurs précis (text, role, data attributes).
    Ne PAS modifier la structure des describe ni les noms des tests — remplacer uniquement le corps (supprimer test.skip(), ajouter le code).
  </read_first>
  <action>
    Remplacer le contenu de e2e/register-non-conseiller.spec.ts.
    Conserver tous les describe/test existants. Implémenter les corps des tests de la section
    "Register page — smoke", "Inscription manager", "Inscription directeur", et "Inscription conseiller — code invitation optionnel".
    Laisser les tests de la section "Wizard conseiller — happy path" skippés (ils seront implémentés en plan 07-02).

    RÈGLES DE CIBLAGE UI :
    - Utiliser `page.getByText("texte exact")` pour les contenus textuels
    - Utiliser `page.getByRole("button", { name: /pattern/i })` pour les boutons
    - Utiliser `page.locator('button').filter({ hasText: "Manager" })` pour les boutons de rôle
    - Timeout par défaut 10_000ms (réseau lent possible)
    - NE PAS appeler `page.fill()` sur email/password dans ces tests (on ne teste pas le submit — juste l'UI)

    IMPLÉMENTATIONS ATTENDUES :

    Section "Register page — smoke" :
    - Test 1 : `page.goto("/register")` → `expect(page.getByText("Créer un compte")).toBeVisible({ timeout: 10_000 })`
    - Test 2 : vérifier que `page.locator("button").filter({ hasText: "Manager" })` est visible

    Section "Inscription manager" :
    - Test 1 "Sélectionner Manager affiche le champ organisation" :
      `page.goto("/register")` → attendre "Créer un compte" → cliquer sur le bouton "Manager" → vérifier que `page.getByText("Organisation")` devient visible
    - Test 2 "Choisir 'Créer une organisation'" :
      Même setup + clic Manager → vérifier que le tab "Créer une organisation" est visible
    - Test 3 "Choisir 'Rejoindre avec un code'" :
      Même setup + clic Manager → cliquer sur "Rejoindre avec un code" → vérifier qu'un `input[placeholder*="AG-"]` ou le placeholder "Ex: AG-1234" devient visible

    Section "Inscription directeur" :
    - Test 1 "Sélectionner Directeur" :
      `page.goto("/register")` → cliquer "Directeur" → vérifier `page.getByText("Organisation")` visible

    Section "Inscription conseiller — code invitation optionnel" :
    - Test 1 "Sans rôle manager, le champ code d'invitation optionnel est visible" :
      `page.goto("/register")` → vérifier que `page.getByText(/Code d.invitation/)` est visible (sans clic supplémentaire — conseiller est sélectionné par défaut)

    IMPORTANT :
    - La section "Wizard conseiller — happy path" reste entièrement skippée (test.skip() conservé sur chaque test)
    - Pas de `// @ts-ignore`
    - Pas de nouvelles dépendances npm
    - Cibler les textes FR réels : "Organisation", "Créer une organisation", "Rejoindre avec un code", etc.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx playwright test e2e/register-non-conseiller.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npx playwright test e2e/register-non-conseiller.spec.ts` : les tests smoke + manager + directeur + conseiller passent (statut "passed").
    Les tests de la section "Wizard conseiller — happy path" sont tous "skipped" (pas "failed").
    Aucune modification de src/app/(auth)/register/page.tsx (vérifier via git diff).
  </done>
</task>

</tasks>

<acceptance_criteria>
```bash
# 1. Aucune modification de register/page.tsx
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && git diff src/app/\(auth\)/register/page.tsx | wc -l
# Doit retourner 0

# 2. Tests passent
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx playwright test e2e/register-non-conseiller.spec.ts --reporter=line 2>&1 | grep -E "passed|failed|skipped"

# 3. Tests wizard still skipped
grep -c "test.skip" /Users/laurentmarx/Documents/Dashboard/NXT-perf/e2e/register-non-conseiller.spec.ts
# Doit retourner >= 5

# 4. TypeScript compile
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit --skipLibCheck 2>&1 | grep "register-non-conseiller" || echo "OK"
```
</acceptance_criteria>

<verification>
1. `npx playwright test e2e/register-non-conseiller.spec.ts` : 0 failures, smoke + non-conseiller = passed, wizard section = skipped
2. `git diff src/app/(auth)/register/page.tsx` : vide (aucune modification)
3. `npx tsc --noEmit --skipLibCheck` : 0 nouvelles erreurs
</verification>

<success_criteria>
- Tests smoke (2), manager (3), directeur (1), conseiller (1) = 7 tests passing sur le code actuel inchangé
- Tests wizard (5) toujours skipped
- register/page.tsx byte-identique avant/après ce plan
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-onboarding-wizard/07-01-SUMMARY.md`
</output>
