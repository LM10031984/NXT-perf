---
phase: 06-training-vocal-module
plan: "02"
type: execute
wave: 3
depends_on:
  - "06-00"
  - "06-01"
files_modified:
  - src/components/training/ScenarioRunner.tsx
  - src/components/training/StepFeedback.tsx
  - src/components/training/SessionSummary.tsx
  - src/app/(dashboard)/conseiller/training/[situation]/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: false
requirements:
  - TRAIN-02
  - TRAIN-03
  - TRAIN-05
  - TRAIN-06

must_haves:
  truths:
    - "L'utilisateur peut cliquer 'Commencer' et entendre le coach parler via ElevenLabs TTS"
    - "L'utilisateur voit un bouton micro actif pendant son tour, peut parler, et voit son transcript affiché"
    - "L'utilisateur voit le score par critère après chaque étape"
    - "À la fin du scénario, l'utilisateur voit le score global et peut relancer"
    - "La sidebar conseiller affiche 'Training vocal' avec icône Mic2"
    - "npx tsc --noEmit vert, npx next build sans erreur"
  artifacts:
    - path: "src/components/training/ScenarioRunner.tsx"
      provides: "Composant principal du runner — monte useScenarioSession et orchestre les vues"
      exports: ["ScenarioRunner"]
    - path: "src/components/training/StepFeedback.tsx"
      provides: "Affichage des critères matchés/non matchés pour une étape"
      exports: ["StepFeedback"]
    - path: "src/components/training/SessionSummary.tsx"
      provides: "Résumé fin de session avec score global et bouton Refaire"
      exports: ["SessionSummary"]
    - path: "src/app/(dashboard)/conseiller/training/[situation]/page.tsx"
      provides: "Page complète branchée sur ScenarioRunner (remplace le placeholder plan 00)"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Nav 'Training vocal' dans section Conseiller"
  key_links:
    - from: "src/app/(dashboard)/conseiller/training/[situation]/page.tsx"
      to: "src/components/training/ScenarioRunner.tsx"
      via: "import { ScenarioRunner }"
      pattern: "ScenarioRunner"
    - from: "src/components/training/ScenarioRunner.tsx"
      to: "src/hooks/use-scenario-session.ts"
      via: "import { useScenarioSession }"
      pattern: "useScenarioSession"
    - from: "src/components/layout/sidebar.tsx"
      to: "/conseiller/training/mandats"
      via: "navItems array entry"
      pattern: "training/mandats"

user_setup: []
---

<objective>
Wave 3 : UI complète + sidebar. Brancher ScenarioRunner sur useScenarioSession, câbler la page, ajouter le nav sidebar.

Purpose: Ce plan ship le flow end-to-end visible utilisateur : bouton Commencer → TTS coach → micro agent → transcript → feedback par critère → résumé → refaire. La sidebar gagne l'entrée "Training vocal".

Output:
- 3 composants training (ScenarioRunner, StepFeedback, SessionSummary)
- Page [situation] mise à jour (remplace le placeholder)
- sidebar.tsx avec nav Training vocal
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-training-vocal-module/06-CONTEXT.md
@.planning/phases/06-training-vocal-module/06-01-SUMMARY.md
@src/types/training.ts
@src/hooks/use-scenario-session.ts
@src/components/layout/sidebar.tsx
@src/app/(dashboard)/conseiller/training/[situation]/page.tsx
</context>

<interfaces>
<!-- Contrats que ce plan consomme — ne pas modifier ces signatures. -->

Depuis src/hooks/use-scenario-session.ts (plan 01) :
```typescript
export function useScenarioSession(scenario: TrainingScenario): {
  state: SessionState;
  currentStep: TrainingStep;
  sessionScore: number;
  dispatch: React.Dispatch<SessionAction>;
  isLastStep: boolean;
  canProceed: boolean;
}
```

Actions dispatch disponibles :
- dispatch({ type: "START" })
- dispatch({ type: "TTS_DONE" })
- dispatch({ type: "RECORDING_DONE", blob: Blob })
- dispatch({ type: "EVALUATION_DONE", evaluation: StepEvaluation })
- dispatch({ type: "NEXT_STEP" })
- dispatch({ type: "SET_ERROR", message: string })
- dispatch({ type: "RESET" })

Depuis src/types/training.ts :
```typescript
type StepPhase = "coach-speaking" | "user-turn" | "evaluating" | "feedback" | "done";
interface StepEvaluation { stepId, transcript, latencyMs, results, score }
```

Depuis src/lib/constants.ts :
```typescript
type SituationType = "mandats" | "estimation" | "objections-acheteur" | "negociation-honoraires" | "follow-up";
```

Sidebar pattern existant (src/components/layout/sidebar.tsx) :
```typescript
interface NavItem {
  href: string;
  icon: React.ComponentType<{ className?: string }>;
  label: string;
  managerOnly?: boolean;
  directorOnly?: boolean;
  networkOnly?: boolean;
  lockedFeature?: string;
  separatorBefore?: boolean;
}
const navItems: NavItem[] = [ ... ]
// Ajouter dans la section conseiller (avant les items managerOnly)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tâche 1 : Composants ScenarioRunner + StepFeedback + SessionSummary</name>
  <files>
    src/components/training/ScenarioRunner.tsx,
    src/components/training/StepFeedback.tsx,
    src/components/training/SessionSummary.tsx
  </files>
  <action>
Créer le répertoire src/components/training/ et les 3 composants suivants.

**src/components/training/StepFeedback.tsx**
Props : `{ evaluation: StepEvaluation }`. Affiche :
- Transcript de l'agent (italique, fond gris léger)
- Liste des critères : icône check vert (Lucide `CheckCircle2`) si matched, icône X rouge (`XCircle`) sinon + label du critère
- Score de l'étape en grand (ex : "7 / 10" ou pourcentage) — utiliser `Math.round(evaluation.score / 10)` / 10 pour un affichage /10 cohérent
- Tailwind uniquement, pas de library UI additionnelle

**src/components/training/SessionSummary.tsx**
Props : `{ evaluations: StepEvaluation[]; sessionScore: number; onRestart: () => void; onExit: () => void }`. Affiche :
- Titre "Scénario terminé !"
- Score global (sessionScore sur 100) avec barre de progression colorée (vert > 70, orange > 40, rouge ≤ 40)
- Résumé par étape (stepId + score)
- Deux boutons : "Recommencer" (appelle onRestart) + "Retour au diagnostic" (Link vers /conseiller/diagnostic)

**src/components/training/ScenarioRunner.tsx**
Props : `{ scenario: TrainingScenario }`. Mount de `useScenarioSession(scenario)`.

Rendu conditionnel selon `state.phase` :

1. `phase === "coach-speaking"` : avatar pulsant (div animée avec `animate-pulse`), texte "Le coach parle..." + `currentStep.coachLine` affiché en léger (l'utilisateur peut lire pendant que ça joue)

2. `phase === "user-turn"` : bouton micro (Lucide `Mic`, rouge, pulsant) centré. Utiliser `MediaRecorder` pour capturer l'audio du microphone :
   - Bouton "Commencer à parler" → demande `navigator.mediaDevices.getUserMedia({ audio: true })`
   - MediaRecorder collecte les chunks
   - Bouton "Terminer" (ou après 60s max) → `recorder.stop()`, construit un Blob, dispatch({ type: "RECORDING_DONE", blob })
   - Afficher un timer en secondes pendant l'enregistrement
   - Si getUserMedia échoue → dispatch({ type: "SET_ERROR", message: "Micro non disponible" })

3. `phase === "evaluating"` : spinner + texte "Transcription en cours..."

4. `phase === "feedback"` : monter `<StepFeedback evaluation={state.evaluations[state.evaluations.length - 1]} />`
   - Si `!isLastStep` : bouton "Étape suivante" → dispatch({ type: "NEXT_STEP" })
   - Si `isLastStep` : dispatch("NEXT_STEP") déclenche déjà la transition vers "done"

5. `phase === "done"` : monter `<SessionSummary evaluations={state.evaluations} sessionScore={sessionScore} onRestart={() => dispatch({ type: "RESET" })} onExit={...} />`

6. `state.error !== null` : bandeau rouge avec le message d'erreur + bouton "Réessayer" → dispatch({ type: "RESET" })

**Section Hero initiale** (avant START) : si `state.phase === "coach-speaking"` et `currentStepIndex === 0` et pas encore commencé, afficher le bouton "Commencer" qui dispatch("START"). Utiliser un flag local `useState<boolean>(false)` pour `hasStarted` — si `!hasStarted`, afficher le hero ; sinon afficher la phase active.

Affichage du progrès : `Étape {currentStepIndex + 1} / {scenario.steps.length}` (afficher seulement le nombre d'étapes avec expectedAgentResponse non null — filtrer les étapes "wrap").

Tailwind + Lucide uniquement. Aucun `// @ts-ignore`. Types stricts.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep -E "(ScenarioRunner|StepFeedback|SessionSummary)" | head -15</automated>
  </verify>
  <done>
    - 3 fichiers créés dans src/components/training/
    - npx tsc --noEmit vert sur ces fichiers
    - Toutes les phases de StepPhase sont gérées (pas de branch oubliée)
    - MediaRecorder correctement typé (utiliser `MediaRecorder` — disponible dans les types DOM de TypeScript)
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Câbler page [situation] + ajouter nav sidebar</name>
  <files>
    src/app/(dashboard)/conseiller/training/[situation]/page.tsx,
    src/components/layout/sidebar.tsx
  </files>
  <action>
**Mise à jour src/app/(dashboard)/conseiller/training/[situation]/page.tsx** (per D2) :

Remplacer le placeholder de plan 00 par la version complète. La page monte ScenarioRunner si situation === "mandats". Garder les branches stub et "introuvable" du plan 00 inchangées.

```typescript
"use client";

import Link from "next/link";
import { type SituationType } from "@/lib/constants";
import { mandatsScenario } from "@/data/training-scenarios/mandats";
import { ScenarioRunner } from "@/components/training/ScenarioRunner";

const VALID_SITUATIONS: SituationType[] = [
  "mandats", "estimation", "objections-acheteur", "negociation-honoraires", "follow-up",
];
const STUB_SITUATIONS: SituationType[] = [
  "estimation", "objections-acheteur", "negociation-honoraires", "follow-up",
];

interface TrainingPageProps {
  params: { situation: string };
}

export default function TrainingPage({ params }: TrainingPageProps) {
  const situation = params.situation as SituationType;

  if (!VALID_SITUATIONS.includes(situation)) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
        <p className="text-muted-foreground">Scénario introuvable.</p>
        <Link href="/conseiller/diagnostic" className="text-sm text-agency-primary underline">
          Retour au diagnostic
        </Link>
      </div>
    );
  }

  if (STUB_SITUATIONS.includes(situation)) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
        <p className="text-lg font-semibold">Ce scénario arrive bientôt</p>
        <p className="text-muted-foreground text-sm">En cours de développement pour une prochaine version.</p>
        <Link href="/conseiller/training/mandats" className="text-sm text-agency-primary underline">
          Essayer le scénario Mandats
        </Link>
      </div>
    );
  }

  // situation === "mandats" — seul scénario opérationnel en Phase 6
  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <div className="mb-6">
        <Link href="/conseiller/diagnostic" className="text-sm text-muted-foreground hover:text-foreground">
          ← Retour
        </Link>
      </div>
      <ScenarioRunner scenario={mandatsScenario} />
    </div>
  );
}
```

**Mise à jour src/components/layout/sidebar.tsx** (per D5) :

1. Ajouter `Mic2` aux imports Lucide en haut du fichier (la liste actuelle importe déjà plusieurs icônes — ajouter `Mic2` dans le destructuring existant).

2. Dans la constante `navItems`, ajouter l'entrée Training vocal APRÈS l'entrée "Mon identité" (href: "/conseiller/identite") et AVANT les items `managerOnly`:

```typescript
{
  href: "/conseiller/training/mandats",
  icon: Mic2,
  label: "Training vocal",
  separatorBefore: true,
},
```

`separatorBefore: true` crée un séparateur visuel discret avant l'entrée, cohérent avec le pattern utilisé pour "Pilotage Financier" dans la section Directeur. Vérifier que la propriété `separatorBefore` est déjà gérée dans `SidebarItem` pour les items conseiller — si non (actuellement géré uniquement dans la section Directeur), ajouter le rendu du séparateur dans la boucle `advisorItems`.

Inspection du code sidebar existant : le séparateur `separatorBefore` n'est rendu que dans la boucle `directorItems`. Pour la section conseiller, ajouter le même pattern dans la boucle `advisorItems` :

```tsx
{advisorItems.map((item) => (
  <Fragment key={item.href}>
    {item.separatorBefore && (
      <div className={cn(
        collapsed ? "my-2 mx-auto h-px w-8 bg-sidebar-border" : "my-2 border-t border-sidebar-border"
      )} />
    )}
    <SidebarItem item={item} pathname={pathname} collapsed={collapsed} />
  </Fragment>
))}
```

Remplacer `advisorItems.map((item) => (...))` par ce pattern avec Fragment. Importer `Fragment` depuis React si pas déjà importé (il l'est déjà en ligne 3 du fichier).
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep -E "(sidebar|training/\[situation\])" | head -10</automated>
  </verify>
  <done>
    - page.tsx monte ScenarioRunner pour situation === "mandats"
    - sidebar.tsx contient l'entrée "Training vocal" pointant vers /conseiller/training/mandats
    - Lucide Mic2 importé et utilisé comme icône
    - npx tsc --noEmit vert sur les 2 fichiers
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "error TS" | head -10
```
Résultat attendu : 0 erreur TypeScript.

Vérification manuelle (checkpoint) — visiter http://localhost:3000/conseiller/training/mandats :
1. La page affiche le titre "Décrocher un mandat exclusif" et un bouton "Commencer"
2. Cliquer "Commencer" → demande d'accès micro → avatar pulsant + première ligne coach lue par TTS
3. Après le TTS → bouton micro rouge apparaît
4. Parler pendant quelques secondes → cliquer "Terminer" → spinner "Transcription..."
5. Feedback affiché avec critères verts/rouges
6. "Étape suivante" → étape 2 se lance (TTS étape 2)
7. La sidebar montre "Training vocal" dans la section Conseiller avec icône micro
</verification>

<success_criteria>
- Flow complet mandats fonctionne de bout en bout : TTS intro → micro → transcript → feedback étape 1 → ... → résumé final
- La sidebar conseiller affiche "Training vocal" avec Mic2 icon et séparateur avant
- /conseiller/training/estimation et les 3 autres stubs affichent "Ce scénario arrive bientôt"
- /conseiller/training/xyz (inconnu) affiche "Scénario introuvable"
- npx tsc --noEmit vert
- npx next build sans erreur
- Aucun `// @ts-ignore` dans les fichiers créés ou modifiés
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-training-vocal-module/06-02-SUMMARY.md` avec :
- Liste des composants créés et leurs props
- Résultat du flow end-to-end testé manuellement (checkpoint)
- Statut npx next build
- Problèmes rencontrés (MediaRecorder, CORS audio, etc.) et solutions

Puis créer `.planning/phases/06-training-vocal-module/06-VERIFICATION.md` qui vérifie les 6 success criteria du ROADMAP Phase 6.
</output>
