---
phase: 06-training-vocal-module
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/training.ts
  - src/data/training-scenarios/mandats.ts
  - src/data/training-scenarios/estimation.ts
  - src/data/training-scenarios/objections-acheteur.ts
  - src/data/training-scenarios/negociation-honoraires.ts
  - src/data/training-scenarios/follow-up.ts
  - src/app/(dashboard)/conseiller/training/[situation]/page.tsx
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-07

must_haves:
  truths:
    - "Les types TrainingScenario, TrainingStep, EvaluationCriteria existent et sont importables depuis @/types/training"
    - "5 fichiers de données existent dans src/data/training-scenarios/ (1 complet + 4 stubs explicites)"
    - "La route /conseiller/training/[situation] répond sans erreur 404 ni erreur de build"
    - "npx tsc --noEmit passe sur tous les nouveaux fichiers"
  artifacts:
    - path: "src/types/training.ts"
      provides: "Interfaces TypeScript du moteur de scénario"
      exports: ["TrainingScenario", "TrainingStep", "EvaluationCriteria", "StepState", "SessionState"]
    - path: "src/data/training-scenarios/mandats.ts"
      provides: "Données complètes scénario mandats (6 étapes)"
      contains: "export const mandatsScenario"
    - path: "src/app/(dashboard)/conseiller/training/[situation]/page.tsx"
      provides: "Page skeleton route dynamique"
      contains: "export default function TrainingPage"
  key_links:
    - from: "src/data/training-scenarios/mandats.ts"
      to: "src/types/training.ts"
      via: "import type { TrainingScenario }"
      pattern: "TrainingScenario"
    - from: "src/app/(dashboard)/conseiller/training/[situation]/page.tsx"
      to: "src/types/training.ts"
      via: "import type { TrainingScenario, SessionState }"
      pattern: "SessionState"
---

<objective>
Wave 0 : poser les contrats de types et les données de scénarios avant d'implémenter le moteur et l'UI.

Purpose: Permettre aux plans 01 et 02 de s'exécuter sans scavenger hunt. Les types définissent exactement ce que le moteur et l'UI attendent ; les 4 scénarios stub lancent une Error explicite pour que le type system attrape toute branche non implémentée.

Output:
- src/types/training.ts — contrats complets
- src/data/training-scenarios/mandats.ts — 6 étapes opérationnelles
- 4 stubs (estimation, objections-acheteur, negociation-honoraires, follow-up)
- Page skeleton route [situation]
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-training-vocal-module/06-CONTEXT.md
@src/lib/constants.ts
@src/lib/personas.ts
</context>

<interfaces>
<!-- Contrats extraits du codebase existant — l'exécuteur n'a pas besoin d'explorer. -->

Depuis src/lib/constants.ts:
```typescript
export type SituationType =
  | "mandats"
  | "estimation"
  | "objections-acheteur"
  | "negociation-honoraires"
  | "follow-up";

export type ElevenLabsPersona = "kind" | "sport" | "warrior";

export const SITUATION_PERSONA_MAP: Record<SituationType, ElevenLabsPersona> = {
  "mandats": "warrior",
  "estimation": "kind",
  "objections-acheteur": "sport",
  "negociation-honoraires": "warrior",
  "follow-up": "kind",
};
```

Depuis src/lib/personas.ts:
```typescript
export type PersonaId = "warrior" | "sport_coach" | "kind_coach";

export const PERSONA_ELEVENLABS_ENV: Record<PersonaId, string> = {
  warrior: "ELEVENLABS_WARRIOR_VOICE_ID",
  sport_coach: "ELEVENLABS_SPORT_COACH_VOICE_ID",
  kind_coach: "ELEVENLABS_KIND_COACH_VOICE_ID",
};
```

NOTE: PersonaId et ElevenLabsPersona sont deux types différents — "sport" dans constants.ts correspond à "sport_coach" dans personas.ts.
Le mapping à utiliser dans training.ts pour résoudre ce décalage :
```typescript
const ELEVENLABS_PERSONA_TO_PERSONA_ID: Record<ElevenLabsPersona, PersonaId> = {
  kind: "kind_coach",
  sport: "sport_coach",
  warrior: "warrior",
};
```

POST /api/voice/tts attend : { text: string, persona?: PersonaId } (voir src/app/api/voice/tts/route.ts)
POST /api/vocal attend : FormData { audio: Blob, section: string }
</interfaces>

<tasks>

<task type="auto">
  <name>Tâche 1 : Définir les types du moteur de scénario (src/types/training.ts)</name>
  <files>src/types/training.ts</files>
  <action>
Créer src/types/training.ts avec les exports suivants (aucun `// @ts-ignore`) :

```typescript
import type { SituationType, ElevenLabsPersona } from "@/lib/constants";
import type { PersonaId } from "@/lib/personas";

/** Critère d'évaluation d'une réponse agent */
export interface EvaluationCriteria {
  key: string;          // identifiant machine ex: "self-intro"
  label: string;        // libellé affiché ex: "Présentation personnelle"
  keywords: string[];   // mots-clés à chercher dans le transcript (insensible à la casse)
}

/** Une étape de scénario */
export interface TrainingStep {
  id: string;
  coachLine: string;                          // texte que le coach lit (TTS)
  expectedAgentResponse: {
    criteria: EvaluationCriteria[];
    minDurationSec: number;
  } | null;                                   // null = fin de scénario (pas de micro attendu)
}

/** Scénario complet */
export interface TrainingScenario {
  id: SituationType;
  title: string;
  description: string;
  persona: ElevenLabsPersona;                 // utilisé pour mapper la voix TTS
  personaId: PersonaId;                       // PersonaId résolu pour /api/voice/tts
  steps: TrainingStep[];
}

/** État d'une étape pendant l'exécution */
export type StepPhase =
  | "coach-speaking"    // TTS en lecture
  | "user-turn"         // micro actif, agent parle
  | "evaluating"        // transcription + évaluation en cours
  | "feedback"          // résultat affiché, attente "Étape suivante"
  | "done";             // fin de scénario

export interface StepEvaluation {
  stepId: string;
  transcript: string;
  latencyMs: number;
  results: Array<{ criteria: EvaluationCriteria; matched: boolean }>;
  score: number;        // 0–100, % de critères matchés
}

/** État de session (géré localement dans useReducer) */
export interface SessionState {
  scenario: TrainingScenario;
  currentStepIndex: number;
  phase: StepPhase;
  evaluations: StepEvaluation[];
  audioBlob: Blob | null;    // enregistrement courant
  error: string | null;
}
```

Vérifier que tous les types s'importent sans erreur circular dependency. Pas de valeur runtime dans ce fichier — uniquement des types et interfaces.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "training.ts" | head -20</automated>
  </verify>
  <done>src/types/training.ts existe, exporte tous les types listés, npx tsc --noEmit ne signale aucune erreur sur ce fichier.</done>
</task>

<task type="auto" tdd="false">
  <name>Tâche 2 : Données scénario mandats + 4 stubs (src/data/training-scenarios/)</name>
  <files>
    src/data/training-scenarios/mandats.ts,
    src/data/training-scenarios/estimation.ts,
    src/data/training-scenarios/objections-acheteur.ts,
    src/data/training-scenarios/negociation-honoraires.ts,
    src/data/training-scenarios/follow-up.ts
  </files>
  <action>
Créer le répertoire src/data/training-scenarios/ et les 5 fichiers suivants.

**src/data/training-scenarios/mandats.ts** — scénario complet (per D1) :

```typescript
import type { TrainingScenario } from "@/types/training";

export const mandatsScenario: TrainingScenario = {
  id: "mandats",
  title: "Décrocher un mandat exclusif",
  description: "Entraîne-toi à convaincre un vendeur hésitant de signer un mandat exclusif.",
  persona: "warrior",
  personaId: "warrior",
  steps: [
    {
      id: "intro",
      coachLine: "Bonjour ! Aujourd'hui on travaille la signature de mandat exclusif. Tu rencontres un vendeur qui hésite entre plusieurs agences. Je joue son rôle. Présente-toi.",
      expectedAgentResponse: {
        criteria: [
          { key: "self-intro", label: "Présentation personnelle", keywords: ["je m'appelle", "je suis", "mon nom", "conseiller", "agent"] },
          { key: "confidence", label: "Ton assuré", keywords: ["plaisir", "enchanté", "ravi", "heureux", "spécialisé"] },
        ],
        minDurationSec: 5,
      },
    },
    {
      id: "objection-prix",
      coachLine: "Votre prix me semble bas par rapport à mes attentes. Un autre agent m'a dit que mon bien vaut 50 000 euros de plus.",
      expectedAgentResponse: {
        criteria: [
          { key: "acknowledge", label: "Accuser réception de l'objection", keywords: ["je comprends", "c'est normal", "cette question", "tout à fait", "légitime"] },
          { key: "evidence", label: "Appuyer sur des données marché", keywords: ["marché", "comparable", "vendu", "données", "prix", "estimation", "valeur"] },
          { key: "redirect-to-strategy", label: "Rediriger vers la stratégie", keywords: ["stratégie", "plan", "accompagnement", "exclusif", "objectif", "résultat"] },
        ],
        minDurationSec: 10,
      },
    },
    {
      id: "objection-exclusivite",
      coachLine: "Pourquoi je donnerais l'exclusivité à une seule agence ? Je préfère ne pas me fermer des portes.",
      expectedAgentResponse: {
        criteria: [
          { key: "mobilisation", label: "Argument mobilisation totale", keywords: ["100%", "mobilisés", "priorité", "focus", "toute", "énergie", "investissement"] },
          { key: "statistiques", label: "Argument statistique", keywords: ["statistiques", "vendent", "plus vite", "délai", "mieux", "résultats", "prouvé"] },
          { key: "engagement", label: "Engagement personnel", keywords: ["je m'engage", "vous garantis", "promets", "compte sur moi", "responsable"] },
        ],
        minDurationSec: 10,
      },
    },
    {
      id: "negociation-duree",
      coachLine: "D'accord, mais je ne veux pas signer pour 3 mois. Maximum 6 semaines.",
      expectedAgentResponse: {
        criteria: [
          { key: "accept-partiel", label: "Accepter le principe sans capituler", keywords: ["comprends", "souci", "flexible", "discutons", "ensemble"] },
          { key: "expliquer-delai", label: "Expliquer pourquoi le délai est nécessaire", keywords: ["délai", "temps", "visibilité", "acheteurs", "optimiser", "plan de vente"] },
        ],
        minDurationSec: 8,
      },
    },
    {
      id: "closing",
      coachLine: "OK. Je vais y réfléchir encore quelques jours.",
      expectedAgentResponse: {
        criteria: [
          { key: "urgency", label: "Créer l'urgence sans pression", keywords: ["marché", "opportunité", "acheteurs", "moment", "profiter", "maintenant", "prêt"] },
          { key: "next-step", label: "Proposer une prochaine étape concrète", keywords: ["rendez-vous", "rappelle", "jeudi", "vendredi", "signer", "avancer", "aujourd'hui"] },
        ],
        minDurationSec: 8,
      },
    },
    {
      id: "wrap",
      coachLine: "Excellent travail ! Tu as géré les objections avec méthode. Points forts : la reconnaissance des objections et l'ancrage sur le marché. Point à travailler : insuffler plus d'urgence bienveillante en closing. On refait ?",
      expectedAgentResponse: null,
    },
  ],
};
```

**src/data/training-scenarios/estimation.ts** — stub :
```typescript
import type { TrainingScenario } from "@/types/training";

/** Scénario "estimation" — non implémenté en Phase 6 (v2). */
export const estimationScenario: TrainingScenario = (() => {
  throw new Error(
    "Scenario 'estimation' not implemented yet. " +
    "Implémentation prévue en Phase 7+ (voir ROADMAP.md v2 requirements)."
  );
})();
```

Répéter le même pattern stub pour objections-acheteur.ts, negociation-honoraires.ts, follow-up.ts — chacun avec le nom de scénario correspondant dans le message d'erreur et l'export nommé respectif (objectionAcheteurScenario, negociationHonorairesScenario, followUpScenario).

NOTE IMPORTANT sur les stubs : l'IIFE qui throw empêche l'import accidentel. Le type system TS attrape tout import non gardé. C'est le comportement voulu per D1.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep -E "(training-scenarios|types/training)" | head -20</automated>
  </verify>
  <done>
    - src/data/training-scenarios/mandats.ts existe avec 6 étapes et tous les critères définis
    - 4 stubs existent, chacun avec un throw Error explicite
    - npx tsc --noEmit vert sur ces fichiers
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : Skeleton page route /conseiller/training/[situation]/page.tsx</name>
  <files>src/app/(dashboard)/conseiller/training/[situation]/page.tsx</files>
  <action>
Créer src/app/(dashboard)/conseiller/training/[situation]/page.tsx comme skeleton "use client" qui :

1. Reçoit `params: { situation: string }` via Next.js App Router
2. Valide que `situation` est une `SituationType` connue (liste exhaustive)
3. Si situation inconnue → affiche un message "Scénario introuvable" avec un lien retour dashboard
4. Si situation = "mandats" → importe mandatsScenario et passe au composant ScenarioRunner (à créer en plan 02)
5. Si situation est un des 4 stubs → affiche "Ce scénario arrive bientôt" (ne pas appeler le stub qui throw)
6. Pour l'instant, sans ScenarioRunner disponible : afficher un placeholder UI avec le titre du scénario, sa description, et "Moteur de scénario en cours de construction (plan 06-01)"

Structure attendue :

```typescript
"use client";

import Link from "next/link";
import { type SituationType, SITUATION_PERSONA_MAP } from "@/lib/constants";
import { mandatsScenario } from "@/data/training-scenarios/mandats";

const VALID_SITUATIONS: SituationType[] = [
  "mandats",
  "estimation",
  "objections-acheteur",
  "negociation-honoraires",
  "follow-up",
];

const STUB_SITUATIONS: SituationType[] = [
  "estimation",
  "objections-acheteur",
  "negociation-honoraires",
  "follow-up",
];

interface TrainingPageProps {
  params: { situation: string };
}

export default function TrainingPage({ params }: TrainingPageProps) {
  const situation = params.situation as SituationType;

  if (!VALID_SITUATIONS.includes(situation)) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
        <p className="text-muted-foreground">Scénario introuvable.</p>
        <Link href="/conseiller/diagnostic" className="text-sm text-agency-primary underline">
          Retour au diagnostic
        </Link>
      </div>
    );
  }

  if (STUB_SITUATIONS.includes(situation)) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4">
        <p className="text-lg font-semibold">Ce scénario arrive bientôt</p>
        <p className="text-muted-foreground text-sm">En cours de développement pour une prochaine version.</p>
        <Link href="/conseiller/training/mandats" className="text-sm text-agency-primary underline">
          Essayer le scénario Mandats
        </Link>
      </div>
    );
  }

  // situation === "mandats"
  const scenario = mandatsScenario;

  return (
    <div className="max-w-2xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold mb-2">{scenario.title}</h1>
      <p className="text-muted-foreground mb-6">{scenario.description}</p>
      {/* ScenarioRunner monté en plan 06-02 */}
      <div className="rounded-lg border border-border bg-muted/30 p-6 text-center text-sm text-muted-foreground">
        Moteur de scénario en cours de construction — plan 06-01
      </div>
    </div>
  );
}
```

Ne pas créer de fichier layout séparé — le layout (dashboard) existant suffit.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "training" | head -20</automated>
  </verify>
  <done>
    - Fichier page.tsx existe sous src/app/(dashboard)/conseiller/training/[situation]/
    - npx tsc --noEmit vert
    - La route /conseiller/training/mandats est accessible (renvoie un 200 en dev)
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20
```
Résultat attendu : 0 erreur sur les fichiers de cette phase.

Vérifier la présence des fichiers :
- src/types/training.ts
- src/data/training-scenarios/mandats.ts (+ 4 stubs)
- src/app/(dashboard)/conseiller/training/[situation]/page.tsx
</verification>

<success_criteria>
- TrainingScenario, TrainingStep, EvaluationCriteria, StepState, SessionState tous exportés depuis @/types/training
- mandatsScenario contient exactement 6 étapes dont la dernière a expectedAgentResponse: null
- 4 stubs existent et throwent une Error explicite (ne pas silencier)
- La page route compile sans erreur TypeScript
- Aucun `// @ts-ignore` dans les fichiers créés
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-training-vocal-module/06-00-SUMMARY.md` avec :
- Fichiers créés et leur rôle
- Décision sur le mapping ElevenLabsPersona ↔ PersonaId documentée
- Statut npx tsc --noEmit
</output>
