---
phase: 06-training-vocal-module
plan: "01"
type: execute
wave: 2
depends_on:
  - "06-00"
files_modified:
  - src/lib/scenario-engine.ts
  - src/hooks/use-scenario-session.ts
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-03
  - TRAIN-05

must_haves:
  truths:
    - "evaluateAgentResponse(transcript, criteria) retourne un score 0-100 déterministe"
    - "useScenarioSession gère le cycle complet : coach-speaking → user-turn → evaluating → feedback → next step"
    - "Le hook appelle POST /api/voice/tts pour lire la ligne du coach"
    - "Le hook appelle transcribeAudio() de src/lib/transcription.ts pour le tour agent"
    - "npx tsc --noEmit vert"
  artifacts:
    - path: "src/lib/scenario-engine.ts"
      provides: "Fonctions pures du moteur : évaluation, score"
      exports: ["evaluateAgentResponse", "computeSessionScore"]
    - path: "src/hooks/use-scenario-session.ts"
      provides: "Hook React gérant l'état de session via useReducer"
      exports: ["useScenarioSession"]
  key_links:
    - from: "src/hooks/use-scenario-session.ts"
      to: "src/lib/scenario-engine.ts"
      via: "import { evaluateAgentResponse }"
      pattern: "evaluateAgentResponse"
    - from: "src/hooks/use-scenario-session.ts"
      to: "src/lib/transcription.ts"
      via: "import { transcribeAudio }"
      pattern: "transcribeAudio"
    - from: "src/hooks/use-scenario-session.ts"
      to: "/api/voice/tts"
      via: "fetch('/api/voice/tts', { method: 'POST', body: JSON.stringify({ text, persona: personaId }) })"
      pattern: "api/voice/tts"
---

<objective>
Wave 1 : implémenter le moteur de scénario (fonctions pures) et le hook de session React.

Purpose: Ce plan produit le cerveau du training — la logique d'évaluation pure testable et le hook useReducer qui orchestre TTS → micro → transcript → feedback. L'UI (plan 02) branchera directement sur ce hook sans logique métier.

Output:
- src/lib/scenario-engine.ts — fonctions pures évaluateAgentResponse + computeSessionScore
- src/hooks/use-scenario-session.ts — hook useReducer complet avec actions dispatch
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-training-vocal-module/06-CONTEXT.md
@.planning/phases/06-training-vocal-module/06-00-SUMMARY.md
@src/types/training.ts
@src/lib/transcription.ts
@src/app/api/voice/tts/route.ts
@src/lib/personas.ts
@src/lib/constants.ts
</context>

<interfaces>
<!-- Contrats que ce plan doit implémenter — extraits des types définis en plan 00. -->

Depuis src/types/training.ts (créé en plan 00) :
```typescript
export interface EvaluationCriteria {
  key: string;
  label: string;
  keywords: string[];
}
export interface TrainingStep {
  id: string;
  coachLine: string;
  expectedAgentResponse: {
    criteria: EvaluationCriteria[];
    minDurationSec: number;
  } | null;
}
export interface TrainingScenario { ... }
export type StepPhase = "coach-speaking" | "user-turn" | "evaluating" | "feedback" | "done";
export interface StepEvaluation {
  stepId: string; transcript: string; latencyMs: number;
  results: Array<{ criteria: EvaluationCriteria; matched: boolean }>;
  score: number;
}
export interface SessionState {
  scenario: TrainingScenario;
  currentStepIndex: number;
  phase: StepPhase;
  evaluations: StepEvaluation[];
  audioBlob: Blob | null;
  error: string | null;
}
```

Depuis src/lib/transcription.ts (Phase 3) :
```typescript
export async function transcribeAudio(
  audio: Blob | File,
  section: string,
  opts?: { timeoutMs?: number }
): Promise<TranscriptionResult>
// TranscriptionResult = { ok: true, text: string, latencyMs: number, raw: unknown }
//                     | { ok: false, error: "rate-limited"|"timeout"|"audio-invalid"|"unknown", details?: string }
```

POST /api/voice/tts attend :
```typescript
{ text: string, persona?: PersonaId }
// PersonaId = "warrior" | "sport_coach" | "kind_coach"
// Retourne audio/mpeg stream
```

Mapping ElevenLabsPersona → PersonaId (résolu en plan 00) :
```typescript
const MAP: Record<ElevenLabsPersona, PersonaId> = {
  kind: "kind_coach",
  sport: "sport_coach",
  warrior: "warrior",
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Moteur d'évaluation pur — src/lib/scenario-engine.ts</name>
  <files>src/lib/scenario-engine.ts, src/lib/__tests__/scenario-engine.test.ts</files>
  <behavior>
    - Test 1 : evaluateAgentResponse("je m'appelle Lucas, ravi de vous rencontrer", [{ key: "self-intro", label: "Présentation", keywords: ["je m'appelle", "je suis"] }, { key: "confidence", label: "Ton assuré", keywords: ["ravi"] }]) → { results: [{criteria, matched: true}, {criteria, matched: true}], score: 100 }
    - Test 2 : evaluateAgentResponse("oui bonjour", [{ key: "self-intro", ..., keywords: ["je m'appelle"] }, { key: "confidence", ..., keywords: ["ravi"] }]) → { results: [{matched: false}, {matched: false}], score: 0 }
    - Test 3 : match partiel → score = 50 (1 critère sur 2)
    - Test 4 : transcript vide → score = 0, tous matched: false
    - Test 5 : keyword insensible à la casse → "JE M'APPELLE" matche keyword "je m'appelle"
    - Test 6 : computeSessionScore([]) → 0
    - Test 7 : computeSessionScore([{score: 80}, {score: 60}]) → 70 (moyenne)
  </behavior>
  <action>
Écrire d'abord src/lib/__tests__/scenario-engine.test.ts avec les 7 tests listés en behavior (RED). Utiliser Vitest (import { describe, it, expect } from "vitest"). Lancer les tests — ils doivent échouer.

Puis implémenter src/lib/scenario-engine.ts (GREEN) :

```typescript
import type { EvaluationCriteria, StepEvaluation } from "@/types/training";

/**
 * Évalue la réponse agent contre une liste de critères.
 * Matching : présence de n'importe quel keyword dans le transcript (insensible à la casse).
 * Retourne score 0-100 = % de critères matchés.
 *
 * Per D3 (CONTEXT.md) : heuristique simple pour v1. Phase 7+ = LLM eval.
 */
export function evaluateAgentResponse(
  transcript: string,
  criteria: EvaluationCriteria[]
): Pick<StepEvaluation, "results" | "score"> {
  if (!transcript.trim() || criteria.length === 0) {
    return {
      results: criteria.map((c) => ({ criteria: c, matched: false })),
      score: 0,
    };
  }

  const lower = transcript.toLowerCase();
  const results = criteria.map((c) => ({
    criteria: c,
    matched: c.keywords.some((kw) => lower.includes(kw.toLowerCase())),
  }));

  const matchedCount = results.filter((r) => r.matched).length;
  const score = criteria.length > 0
    ? Math.round((matchedCount / criteria.length) * 100)
    : 0;

  return { results, score };
}

/**
 * Calcule le score agrégé de session (moyenne des scores par étape).
 */
export function computeSessionScore(evaluations: Pick<StepEvaluation, "score">[]): number {
  if (evaluations.length === 0) return 0;
  const total = evaluations.reduce((sum, e) => sum + e.score, 0);
  return Math.round(total / evaluations.length);
}
```

Relancer les tests — ils doivent passer (GREEN). Pas de refactoring nécessaire ici.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx vitest run src/lib/__tests__/scenario-engine.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>7 tests passent, 0 échec. npx tsc --noEmit vert sur scenario-engine.ts.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Hook useScenarioSession — src/hooks/use-scenario-session.ts</name>
  <files>src/hooks/use-scenario-session.ts</files>
  <action>
Créer src/hooks/use-scenario-session.ts. Ce hook encapsule tout l'état de session via useReducer. Il expose une interface simple à l'UI — zéro logique métier dans les composants.

**Actions du reducer :**
```typescript
type SessionAction =
  | { type: "START" }                                  // passe à coach-speaking, lance TTS étape 0
  | { type: "TTS_DONE" }                               // passe à user-turn
  | { type: "RECORDING_DONE"; blob: Blob }             // stocke audioBlob, passe à evaluating
  | { type: "EVALUATION_DONE"; evaluation: StepEvaluation } // stocke eval, passe à feedback
  | { type: "NEXT_STEP" }                              // avance l'index ou passe à done
  | { type: "SET_ERROR"; message: string }             // stocke error, reste dans phase courante
  | { type: "RESET" }                                  // retour à état initial (phase: "coach-speaking")
```

**État initial :**
```typescript
{
  scenario,
  currentStepIndex: 0,
  phase: "coach-speaking",  // commence directement — START lance le TTS
  evaluations: [],
  audioBlob: null,
  error: null,
}
```

**Effets (useEffect) :**

1. `phase === "coach-speaking"` → appeler `playCoachLine(currentStep.coachLine, scenario.personaId)`:
   - fetch POST /api/voice/tts avec `{ text: coachLine, persona: personaId }`
   - Récupérer response.blob(), créer un `new Audio(URL.createObjectURL(blob))`, appeler `.play()`
   - À la fin de l'audio (event `ended`) → dispatch("TTS_DONE")
   - Si le step a `expectedAgentResponse === null` → dispatch("TTS_DONE") directement (pas d'audio attendu)
   - En cas d'erreur fetch → dispatch("SET_ERROR", message)

2. `phase === "evaluating"` → appeler `runEvaluation()`:
   - Appeler `transcribeAudio(audioBlob, scenario.id, { timeoutMs: 15_000 })`
   - Si result.ok === false → dispatch("SET_ERROR", `Transcription échouée : ${result.error}`)
   - Si result.ok → calculer evaluation via `evaluateAgentResponse(result.text, currentStep.expectedAgentResponse!.criteria)`
   - Construire StepEvaluation et dispatch("EVALUATION_DONE", evaluation)

**Valeurs retournées par le hook :**
```typescript
return {
  state,                   // SessionState complet
  currentStep,             // TrainingStep courant
  sessionScore,            // computeSessionScore(state.evaluations)
  dispatch,                // pour que l'UI déclenche les actions
  // Helpers
  isLastStep: currentStepIndex === scenario.steps.length - 1,
  canProceed: phase === "feedback" || (phase === "coach-speaking" && currentStep.expectedAgentResponse === null),
};
```

**Contraintes impératives :**
- Aucun `// @ts-ignore`
- Cleanup propre dans useEffect (ref sur l'objet Audio, abort sur le fetch si le composant démonte)
- Ne pas utiliser `any` — typer explicitement `response: Response`, `blob: Blob`
- Le hook accepte `scenario: TrainingScenario` en paramètre — pas de logique de sélection ici
- Importer `evaluateAgentResponse`, `computeSessionScore` depuis `@/lib/scenario-engine`
- Importer `transcribeAudio` depuis `@/lib/transcription`

**Note sur l'audio playback :**
`new Audio(...)` est valide côté client uniquement. Protéger avec `typeof window !== "undefined"` si nécessaire. Dans les effets React, le code s'exécute côté client donc OK.
  </action>
  <verify>
    <automated>cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx tsc --noEmit 2>&1 | grep "use-scenario-session" | head -10</automated>
  </verify>
  <done>
    - src/hooks/use-scenario-session.ts existe sans erreur TypeScript
    - Toutes les actions du reducer sont typées et implémentées
    - Les 2 effets (TTS + évaluation) sont présents avec cleanup
    - Aucun `any` ni `// @ts-ignore`
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/laurentmarx/Documents/Dashboard/NXT-perf && npx vitest run src/lib/__tests__/scenario-engine.test.ts && npx tsc --noEmit 2>&1 | grep "error TS" | head -10
```
Résultat attendu : 7 tests passent, 0 erreur TypeScript.
</verification>

<success_criteria>
- evaluateAgentResponse retourne score 0-100 déterministe basé sur keyword presence
- computeSessionScore calcule la moyenne des scores d'évaluation
- useScenarioSession orchestre les transitions d'état et les effets TTS/STT
- Le hook ne contient aucune logique UI (pas de JSX, pas d'import de composants)
- npx tsc --noEmit vert sur tous les fichiers de cette phase
</success_criteria>

<output>
Après complétion, créer `.planning/phases/06-training-vocal-module/06-01-SUMMARY.md` avec :
- Signature exacte de evaluateAgentResponse et computeSessionScore
- Liste complète des actions SessionAction
- Résultat npx vitest run (N/N tests passés)
- Tout problème de typage rencontré et résolu
</output>
