---
phase: 01-context-layer-data-hygiene
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/copilot-context.test.ts
  - src/lib/__tests__/data-access.test.ts
  - src/lib/__tests__/constants.test.ts
  - src/stores/__tests__/copilot-store-isolation.test.ts
  - src/app/api/copilot/rag-health/route.ts
autonomous: true
requirements:
  - COPILOT-07
  - COPILOT-08
  - DATA-01
  - TRAIN-04
  - RAG-05

must_haves:
  truths:
    - "Test runner has at least one file referencing each Phase 1 production module"
    - "`npx vitest run` exits 0 (skipped tests count as passing) before Wave 1 implementation begins"
    - "`npx tsc --noEmit` passes with rag-health route stub returning a typed response"
    - "Nyquist sampling: every Wave 1 task has its test file already present"
  artifacts:
    - path: "src/lib/__tests__/copilot-context.test.ts"
      provides: "Skipped describe blocks for COPILOT-07 token cap + field exclusion"
      contains: "describe.skip"
    - path: "src/lib/__tests__/data-access.test.ts"
      provides: "Skipped describe blocks for DATA-01 null + match cases"
      contains: "describe.skip"
    - path: "src/lib/__tests__/constants.test.ts"
      provides: "Skipped describe blocks for TRAIN-04 SITUATION_PERSONA_MAP completeness"
      contains: "describe.skip"
    - path: "src/stores/__tests__/copilot-store-isolation.test.ts"
      provides: "Skipped grep-based isolation test for COPILOT-08"
      contains: "describe.skip"
    - path: "src/app/api/copilot/rag-health/route.ts"
      provides: "Typed stub GET handler returning 501 Not Implemented"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/copilot/rag-health/route.ts"
      to: "Next.js App Router"
      via: "export async function GET"
      pattern: "export async function GET"
---

<objective>
Create Wave 0 test stubs and the rag-health route stub so the test runner and TypeScript compiler can sample EVERY subsequent task in Phase 1 from commit #1. No production logic is implemented here — only `describe.skip` blocks and a typed 501 stub route.

Purpose: Satisfy the Nyquist principle laid out in VALIDATION.md — sampling rate must exceed implementation pace. If test files don't exist before implementation lands, the first commit of Wave 1 has no automated feedback signal.

Output: 4 test files containing `describe.skip` blocks + 1 route stub. `npx vitest run` and `npx tsc --noEmit` both pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-context-layer-data-hygiene/01-CONTEXT.md
@.planning/phases/01-context-layer-data-hygiene/01-RESEARCH.md
@.planning/phases/01-context-layer-data-hygiene/01-VALIDATION.md
@src/lib/__tests__/weekly-gate.test.ts
@vitest.config.ts

<interfaces>
<!-- Conventions for test files in this project (from src/lib/__tests__/weekly-gate.test.ts) -->

Test file template (Vitest 4.1.2, `npx vitest run` runner):
```typescript
import { describe, it, expect } from "vitest";

describe.skip("Module: brief description (FRENCH OK)", () => {
  it("decrit ce que le test couvrira (stub Wave 0)", () => {
    expect(true).toBe(true);
  });
});
```

Path alias confirmed: `@/*` → `./src/*` (vitest.config.ts).

Next.js App Router GET stub pattern:
```typescript
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json(
    { ok: false, error: "Not implemented yet", chunks: 0, syntheses: 0 },
    { status: 501 }
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Create copilot-context test stub (COPILOT-07)</name>
  <files>src/lib/__tests__/copilot-context.test.ts</files>
  <read_first>
    - src/lib/__tests__/weekly-gate.test.ts (Vitest + French descriptions convention)
    - .planning/phases/01-context-layer-data-hygiene/01-VALIDATION.md (Wave 0 gap list)
  </read_first>
  <action>
    Create `src/lib/__tests__/copilot-context.test.ts`. File body:

    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("buildCopilotContext — COPILOT-07 token cap", () => {
      it("caps total payload at 3000 tokens (Wave 0 stub)", () => {
        // Implemented in plan 01-01 — see CONTEXT.md D1/D2.
        expect(true).toBe(true);
      });

      it("ne sérialise jamais le champ users[] (Wave 0 stub)", () => {
        // Implemented in plan 01-01 — exclusion list per CONTEXT.md D1.
        expect(true).toBe(true);
      });

      it("ne sérialise jamais le champ networks (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("ne sérialise jamais le champ financialData (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });
    ```

    Use `describe.skip` (not `it.skip`) so the entire group is reported as skipped, not individual cases. NO production import — Wave 0 must not depend on yet-unwritten modules.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/copilot-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `src/lib/__tests__/copilot-context.test.ts`
    - Contains literal string `describe.skip("buildCopilotContext — COPILOT-07 token cap"`
    - Contains 4 `it(...)` cases as listed above (verify with `grep -c "it(" src/lib/__tests__/copilot-context.test.ts` → returns 4)
    - No import statement from `@/lib/copilot-context` or `@/types/copilot` (these don't exist yet)
    - `npx vitest run src/lib/__tests__/copilot-context.test.ts` reports "skipped" without errors
  </acceptance_criteria>
  <done>Test file present, 4 stub cases inside `describe.skip`, vitest reports skip without error.</done>
</task>

<task type="auto">
  <name>Task 0.2: Create data-access + constants + copilot-store-isolation test stubs (DATA-01, TRAIN-04, COPILOT-08)</name>
  <files>
    src/lib/__tests__/data-access.test.ts,
    src/lib/__tests__/constants.test.ts,
    src/stores/__tests__/copilot-store-isolation.test.ts
  </files>
  <read_first>
    - src/lib/__tests__/weekly-gate.test.ts (convention reference)
    - .planning/phases/01-context-layer-data-hygiene/01-VALIDATION.md
  </read_first>
  <action>
    Create three files (parallel-safe — independent test suites).

    **File 1: `src/lib/__tests__/data-access.test.ts`** —
    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("getWeeklyResults — DATA-01", () => {
      it("renvoie null quand aucun résultat ne correspond (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("renvoie le PeriodResults correct pour un couple (userId, period) connu (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("matche period au format YYYY-MM sur periodStart.startsWith (Wave 0 stub)", () => {
        // Convention décidée plan 01-02 : period = "YYYY-MM" → periodStart.startsWith(period).
        expect(true).toBe(true);
      });
    });
    ```

    **File 2: `src/lib/__tests__/constants.test.ts`** —
    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("SITUATION_PERSONA_MAP — TRAIN-04", () => {
      it("contient les 5 situations (mandats, estimation, objections-acheteur, negociation-honoraires, follow-up) (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("mappe chaque situation vers une persona valide (kind|sport|warrior) (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("PERSONA_VOICE_ENV_VAR contient les 3 personas (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });
    ```

    **File 3: `src/stores/__tests__/copilot-store-isolation.test.ts`** — create the `src/stores/__tests__/` directory if missing.
    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("copilot-store isolation — COPILOT-08", () => {
      it("app-store.ts ne référence jamais copilot-store (Wave 0 stub)", () => {
        // Implementé plan 01-03 — grep via fs.readFileSync sur src/stores/app-store.ts.
        expect(true).toBe(true);
      });
    });
    ```

    All three files: French descriptions use real characters (é, è, à). NO production imports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/data-access.test.ts src/lib/__tests__/constants.test.ts src/stores/__tests__/copilot-store-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at exact paths listed in `<files>`
    - `data-access.test.ts` contains `describe.skip("getWeeklyResults — DATA-01"` and 3 `it(...)` cases
    - `constants.test.ts` contains `describe.skip("SITUATION_PERSONA_MAP — TRAIN-04"` and 3 `it(...)` cases
    - `copilot-store-isolation.test.ts` contains `describe.skip("copilot-store isolation — COPILOT-08"` and 1 `it(...)` case
    - French characters are literal UTF-8 (verify: `grep -c "é\|è\|à" src/lib/__tests__/data-access.test.ts` returns > 0)
    - No `\u00` Unicode escape in any file (`grep -c "\\\\u00" src/lib/__tests__/data-access.test.ts src/lib/__tests__/constants.test.ts src/stores/__tests__/copilot-store-isolation.test.ts` returns 0)
    - `npx vitest run` (full) reports the 7 cases as skipped and exits 0
  </acceptance_criteria>
  <done>Three test files present, all `describe.skip`, vitest reports skip without error.</done>
</task>

<task type="auto">
  <name>Task 0.3: Create rag-health route stub (RAG-05 sampling continuity)</name>
  <files>src/app/api/copilot/rag-health/route.ts</files>
  <read_first>
    - .planning/phases/01-context-layer-data-hygiene/01-CONTEXT.md (D6 contract)
    - .planning/phases/01-context-layer-data-hygiene/01-RESEARCH.md (Pattern 4 final shape)
  </read_first>
  <action>
    Create `src/app/api/copilot/rag-health/route.ts` containing ONLY a typed 501 Not Implemented stub. Full retrieval wiring lands in plan 01-05.

    ```typescript
    import { NextResponse } from "next/server";

    // STUB — Wave 0. Full implementation in plan 01-05 (see CONTEXT.md D6).
    // Présence du fichier nécessaire pour que `npx tsc --noEmit` couvre la route
    // dès le premier commit de la Phase 1.
    export async function GET(): Promise<NextResponse> {
      return NextResponse.json(
        {
          ok: false,
          error: "Not implemented yet (Wave 0 stub — see plan 01-05)",
          chunks: 0,
          syntheses: 0,
        },
        { status: 501 },
      );
    }
    ```

    Why: this stub guarantees `npx tsc --noEmit` covers the route from commit 1. Plan 01-05 replaces the body with the real handler — file path stays unchanged so frontmatter `files_modified` remains accurate.

    Do NOT import `retrieveHybrid`, `requireAuth`, or `checkRateLimit` here — those imports belong to plan 01-05.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `src/app/api/copilot/rag-health/route.ts`
    - Contains literal string `export async function GET`
    - Contains literal string `status: 501`
    - Contains literal string `Wave 0 stub`
    - Does NOT import `retrieveHybrid` (verify: `grep -c "retrieveHybrid" src/app/api/copilot/rag-health/route.ts` returns 0)
    - Does NOT contain `// @ts-ignore` or `// @ts-expect-error` (verify: returns 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Stub route returns 501 with typed body, no production deps imported, tsc passes.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npx vitest run` exits 0 (4 test files report skipped suites, total 8 stub cases)
- `npx tsc --noEmit` exits 0
- `npx next lint` exits 0
- `grep -c "describe.skip" src/lib/__tests__/copilot-context.test.ts src/lib/__tests__/data-access.test.ts src/lib/__tests__/constants.test.ts src/stores/__tests__/copilot-store-isolation.test.ts` returns 4 (one per file)
</verification>

<success_criteria>
1. All 4 Wave 0 gaps from VALIDATION.md closed (3 test stubs + rag-health route stub)
2. Test runner produces signal for every Wave 1 implementation file from its first commit
3. No production logic introduced — strictly scaffolding
4. Lint + tsc + vitest all exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-context-layer-data-hygiene/01-00-SUMMARY.md` documenting:
- The 5 files created
- The convention `period = "YYYY-MM"` recorded in test names (downstream contract)
- The `describe.skip` strategy (consumed by plans 01-01, 01-02, 01-03, 01-04, 01-05 which flip `.skip` to active as they implement)
</output>
