# Phase 3 — VocalFlow Stabilization — Validation Checklist

**Phase:** 03-vocalflow-stabilization
**Requirements:** VOICE-01, VOICE-02, VOICE-03, VOICE-04
**Generated:** 2026-05-21

---

## Must-Haves (goal-backward)

### Observable Truths (user perspective)

| # | Truth | Grep / Command | Expected |
|---|-------|----------------|----------|
| T1 | VocalFlow bugs are documented | `cat .planning/phases/03-vocalflow-stabilization/BUGS.md \| grep -c "^## BUG"` | ≥ 3 |
| T2 | No blocker bug is open | `grep -A5 "Severity.*blocker" .planning/phases/03-vocalflow-stabilization/BUGS.md \| grep -c "Fixed"` | ≥ 1 (all blockers fixed) |
| T3 | transcription.ts wrapper implemented | `grep "AbortController" src/lib/transcription.ts` | match |
| T4 | Retry on 429 present | `grep "429" src/lib/transcription.ts` | match |
| T5 | Timeout configurable | `grep "timeoutMs" src/lib/transcription.ts` | match |
| T6 | Hook uses wrapper, not inline fetch | `grep "fetch.*api/vocal" src/hooks/use-vocal-flow.ts` | 0 matches |
| T7 | Dashboard CTA exists | `grep "Saisir mes chiffres à la voix" src/components/vocal/VocalDrawer.tsx` | match |
| T8 | CTA conditionally rendered | `grep "submissionStatus" src/components/vocal/VocalDrawer.tsx` | match |
| T9 | E2E spec exists with active tests | `grep -c "test.skip" e2e/saisie-vocale.spec.ts` | ≤ 3 (MediaRecorder tests only) |
| T10 | E2E covers CTA | `grep "Saisir mes chiffres" e2e/saisie-vocale.spec.ts` | match |

### Required Artifacts

| File | Provides | Min Verification |
|------|----------|-----------------|
| `.planning/phases/03-vocalflow-stabilization/BUGS.md` | Bug inventory | `grep -c "## BUG" BUGS.md` ≥ 3 |
| `src/lib/transcription.ts` | Typed Result + retry + timeout | File exists, `wc -l` ≥ 40 |
| `src/hooks/use-vocal-flow.ts` | Updated processAudio | `grep "transcribeAudio"` match |
| `src/components/vocal/VocalFlow.tsx` | Bug fixes applied | `grep "flow.startFlow" src/components/vocal/VocalFlow.tsx \| grep "catch"` = 0 |
| `src/components/vocal/VocalDrawer.tsx` | Dashboard CTA component | File exists |
| `e2e/saisie-vocale.spec.ts` | E2E safety net | `npx playwright test e2e/saisie-vocale.spec.ts` exits 0 |

### Key Wiring Links

| From | To | Via | Grep |
|------|----|-----|------|
| `use-vocal-flow.ts` | `transcription.ts` | `import { transcribeAudio }` | `grep "from.*@/lib/transcription" src/hooks/use-vocal-flow.ts` |
| `VocalDrawer.tsx` | `use-weekly-gate.ts` | `useWeeklyGate()` | `grep "useWeeklyGate" src/components/vocal/VocalDrawer.tsx` |
| `VocalDrawer.tsx` | `VocalFlow.tsx` | `<VocalFlow />` | `grep "VocalFlow" src/components/vocal/VocalDrawer.tsx` |
| Dashboard page | `VocalDrawer.tsx` | `<VocalDrawer />` | `grep "VocalDrawer" src/app/(dashboard)/dashboard/page.tsx` |
| `transcription.ts` | `/api/vocal` | `fetch("/api/vocal"` | `grep "api/vocal" src/lib/transcription.ts` |

---

## Automated Validation Suite

Run all commands from project root:

```bash
# 1. TypeScript — zero errors
npx tsc --noEmit
# Expected: exits 0, no output

# 2. Unit tests — no regressions
npx vitest run
# Expected: exits 0, all tests pass

# 3. E2E — saisie vocale spec
npx playwright test e2e/saisie-vocale.spec.ts
# Expected: exits 0 (4 pass + 3 skip = 0 fail)

# 4. E2E — existing saisie spec not broken
npx playwright test e2e/saisie-page.spec.ts
# Expected: exits 0

# 5. Wrapper contract
grep "export type TranscriptionResult" src/lib/transcription.ts
grep "export async function transcribeAudio" src/lib/transcription.ts
# Expected: both return matches

# 6. No inline fetch in hook
grep "fetch.*api/vocal" src/hooks/use-vocal-flow.ts | wc -l
# Expected: 0

# 7. Bug inventory complete
cat .planning/phases/03-vocalflow-stabilization/BUGS.md | grep -c "^## BUG"
# Expected: ≥ 3
```

---

## Requirement Coverage

| Requirement | Plan | Covered By |
|-------------|------|-----------|
| VOICE-01 — Bugs documented and fixed | 03-01 | BUGS.md + VocalFlow.tsx fixes |
| VOICE-02 — transcription.ts wrapper | 03-00 (skeleton) + 03-01 (impl) | `src/lib/transcription.ts` |
| VOICE-03 — Dashboard CTA | 03-02 | `src/components/vocal/VocalDrawer.tsx` + dashboard page |
| VOICE-04 — Playwright E2E spec | 03-00 (scaffold) + 03-02 (fill) | `e2e/saisie-vocale.spec.ts` |

---

## Phase 3 Definition of Done (from 03-CONTEXT.md)

- [ ] BUGS.md catalogs every observed VocalFlow defect with fixes documented
- [ ] All blocker + major bugs fixed and committed atomically
- [ ] `src/lib/transcription.ts` exists, typed Result API, retry + timeout
- [ ] VocalFlow.tsx uses the wrapper (no inline `fetch` to Groq)
- [ ] New E2E spec at `e2e/saisie-vocale.spec.ts` covers the full flow end-to-end
- [ ] Dashboard CTA exists and renders when appropriate
- [ ] `npx tsc --noEmit` + `npx vitest run` + `npx playwright test e2e/saisie-vocale.spec.ts` all green

---

*Validation generated by plan-phase agent — 2026-05-21*
