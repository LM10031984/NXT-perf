---
phase: 02-streaming-api-rag-grounding
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/server/coach-rag/__tests__/system-prompt.test.ts
  - src/app/api/copilot/__tests__/stream.test.ts
  - src/app/api/copilot/stream/route.ts
autonomous: true
requirements:
  - COPILOT-04
  - COPILOT-06
  - COPILOT-09
  - COPILOT-10
  - COPILOT-11
  - RAG-02
  - RAG-03
  - RAG-04
  - RAG-06

must_haves:
  truths:
    - "Test runner has at least one file referencing each Phase 2 production module before Wave 1 lands"
    - "`npx vitest run` exits 0 (skipped tests count as passing) before Wave 1 implementation begins"
    - "`npx tsc --noEmit` passes with the streaming route stub returning a typed 501 response"
    - "Nyquist sampling: every Wave 1+ task has its test file already present"
  artifacts:
    - path: "src/lib/server/coach-rag/__tests__/system-prompt.test.ts"
      provides: "Skipped describe blocks for formatUserContext + <rag-source> wrapping + <contract-policy> + <grounding-state>"
      contains: "describe.skip"
    - path: "src/app/api/copilot/__tests__/stream.test.ts"
      provides: "Skipped describe blocks for stream route (demo short-circuit, auth, rate-limit, timeout, SSE shape)"
      contains: "describe.skip"
    - path: "src/app/api/copilot/stream/route.ts"
      provides: "Typed stub POST handler returning 501 Not Implemented (build-gate sampling)"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/copilot/stream/route.ts"
      to: "Next.js App Router"
      via: "export async function POST"
      pattern: "export async function POST"
    - from: "src/lib/server/coach-rag/__tests__/system-prompt.test.ts"
      to: "src/lib/server/coach-rag/system-prompt.ts (existing)"
      via: "future import buildSystemPrompt + formatUserContext"
      pattern: "describe.skip"
---

<objective>
Create Wave 0 test stubs and the streaming route stub so the test runner and TypeScript compiler can sample EVERY subsequent task in Phase 2 from commit #1. No production logic is implemented here — only `describe.skip` blocks and a typed 501 stub route.

Purpose: Satisfy the Nyquist principle laid out in VALIDATION.md — the sampling rate must exceed the implementation pace. If test files don't exist before implementation lands, the first commit of Wave 1 has no automated feedback signal.

Output: 2 test files containing `describe.skip` blocks + 1 route stub. `npx vitest run` and `npx tsc --noEmit` both pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-api-rag-grounding/02-CONTEXT.md
@.planning/phases/02-streaming-api-rag-grounding/02-VALIDATION.md
@src/lib/__tests__/copilot-context.test.ts
@vitest.config.ts

<interfaces>
<!-- Conventions for test files in this project (proven by Phase 1 plan 01-00) -->

Test file template (Vitest 4.1.2, `npx vitest run` runner):
```typescript
import { describe, it, expect } from "vitest";

describe.skip("Module: brief description (FRENCH OK)", () => {
  it("decrit ce que le test couvrira (stub Wave 0)", () => {
    expect(true).toBe(true);
  });
});
```

Path alias confirmed: `@/*` → `./src/*` (vitest.config.ts).

Next.js App Router POST stub pattern (mirrors Phase 1 GET stub for rag-health):
```typescript
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json(
    { error: "Not implemented yet" },
    { status: 501 }
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Create system-prompt test stub (RAG-04, RAG-06, COPILOT-10)</name>
  <files>src/lib/server/coach-rag/__tests__/system-prompt.test.ts</files>
  <read_first>
    - src/lib/__tests__/copilot-context.test.ts (Vitest + French descriptions convention)
    - .planning/phases/02-streaming-api-rag-grounding/02-VALIDATION.md (Wave 0 gap list)
    - src/lib/server/coach-rag/system-prompt.ts (existing exports — DO NOT modify in this task)
  </read_first>
  <action>
    Create the directory `src/lib/server/coach-rag/__tests__/` if it does not exist.

    Create `src/lib/server/coach-rag/__tests__/system-prompt.test.ts`. Exact file body:

    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("formatUserContext — RAG-04 user context block (Wave 0 stub, plan 02-01)", () => {
      it("renders the catégorie line in French (Junior|Confirmé|Expert) (Wave 0 stub)", () => {
        // Implemented in plan 02-01 — see CONTEXT.md D3 format.
        expect(true).toBe(true);
      });

      it("renders the topCriticite name+diagnosis line (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("returns empty string when userContext is undefined (backward compat) (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });

    describe.skip("buildSystemPrompt — RAG-06 <rag-source> wrapping (Wave 0 stub, plan 02-01)", () => {
      it("wraps each chunk inside <rag-source>...</rag-source> when chunks present (Wave 0 stub)", () => {
        // Per PITFALLS I-1 + CONTEXT.md D3.
        expect(true).toBe(true);
      });

      it("includes the prompt-injection defense instruction string referencing rag-source tags (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });

    describe.skip("buildSystemPrompt — COPILOT-10 <contract-policy> loi Hoguet (Wave 0 stub, plan 02-01)", () => {
      it("always includes a <contract-policy> block mentioning loi Hoguet (Wave 0 stub)", () => {
        // Per CONTEXT.md D6 — non-negotiable.
        expect(true).toBe(true);
      });

      it("instructs the LLM to redirect price-estimation requests to the outil agréé (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });

    describe.skip("buildSystemPrompt — RAG-04 <grounding-state> zero-grounding (Wave 0 stub, plan 02-01)", () => {
      it("emits <grounding-state>none</grounding-state> when chunks+syntheses empty (Wave 0 stub)", () => {
        // Per CONTEXT.md D5.
        expect(true).toBe(true);
      });

      it("does NOT emit <grounding-state>none</grounding-state> when at least one chunk passes the threshold (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("instructs the LLM to say 'Je n'ai pas d'exemple pertinent' on zero-grounding (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });
    ```

    Use `describe.skip` (not `it.skip`) so the entire group is reported skipped, not individual cases. NO production import — Wave 0 must not depend on yet-extended modules.
  </action>
  <verify>
    <automated>npx vitest run src/lib/server/coach-rag/__tests__/system-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `src/lib/server/coach-rag/__tests__/system-prompt.test.ts`
    - Contains 4 distinct `describe.skip(` strings (verify: `grep -c "describe.skip(" src/lib/server/coach-rag/__tests__/system-prompt.test.ts` returns 4)
    - Contains at least 10 `it(` cases total (verify: `grep -c "  it(" src/lib/server/coach-rag/__tests__/system-prompt.test.ts` returns ≥ 10)
    - Contains the literal phrase `<grounding-state>` somewhere (forward-defining the zero-grounding contract per D5)
    - Contains the literal phrase `<contract-policy>` somewhere (forward-defining the Hoguet block per D6)
    - Contains the literal phrase `<rag-source>` somewhere (forward-defining the wrapping per D3 + I-1)
    - No import statement from `../system-prompt` (production module is untouched in Wave 0)
    - `npx vitest run src/lib/server/coach-rag/__tests__/system-prompt.test.ts` reports "skipped" without errors
  </acceptance_criteria>
  <done>Test file present, 4 stub describe blocks, ≥10 stub cases, vitest reports skip without error.</done>
</task>

<task type="auto">
  <name>Task 0.2: Create stream route test stub (COPILOT-04, COPILOT-06, COPILOT-09, COPILOT-11, RAG-02, RAG-03)</name>
  <files>src/app/api/copilot/__tests__/stream.test.ts</files>
  <read_first>
    - src/lib/__tests__/copilot-context.test.ts (convention reference)
    - .planning/phases/02-streaming-api-rag-grounding/02-VALIDATION.md (Wave 0 gap list)
    - .planning/phases/02-streaming-api-rag-grounding/02-CONTEXT.md (D4, D5, D7 — observable behaviors to stub)
  </read_first>
  <action>
    Create the directory `src/app/api/copilot/__tests__/` if it does not exist.

    Create `src/app/api/copilot/__tests__/stream.test.ts`. Exact file body:

    ```typescript
    import { describe, it, expect } from "vitest";

    describe.skip("POST /api/copilot/stream — auth + rate-limit (COPILOT-09, Wave 0 stub, plan 02-02)", () => {
      it("returns 401 when unauthenticated (Wave 0 stub)", () => {
        // Implemented in plan 02-02 — copies the rag-health auth pattern.
        expect(true).toBe(true);
      });

      it("returns 429 after 10 calls in 60s for the same user (Wave 0 stub)", () => {
        // Per CONTEXT.md D7.
        expect(true).toBe(true);
      });

      it("returns 405 on GET (Wave 0 stub)", () => {
        // Per CONTEXT.md Specifics — POST only.
        expect(true).toBe(true);
      });
    });

    describe.skip("POST /api/copilot/stream — demo short-circuit (COPILOT-06, Wave 0 stub, plan 02-02)", () => {
      it("when X-Demo-Mode header = true, returns a stubbed SSE stream without calling OpenRouter (Wave 0 stub)", () => {
        // Per CONTEXT.md D4. The body must NOT contain real coaching content.
        expect(true).toBe(true);
      });

      it("the demo stub stream terminates with data: [DONE] (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });

    describe.skip("POST /api/copilot/stream — SSE shape (COPILOT-04, Wave 0 stub, plan 02-02)", () => {
      it("Content-Type response header equals text/event-stream (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("emits at least one data: line during streaming (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });

    describe.skip("POST /api/copilot/stream — RAG threshold + grounding (RAG-02, RAG-03, Wave 0 stub, plan 02-02)", () => {
      it("filters chunks below 0.75 cosine similarity before building the system prompt (Wave 0 stub)", () => {
        // Per CONTEXT.md D5.
        expect(true).toBe(true);
      });

      it("emits a Source: [filename] reference when at least one strong chunk is returned (Wave 0 stub)", () => {
        // Per RAG-02 — handled at prompt-shape level (LLM must cite given a Source: line).
        expect(true).toBe(true);
      });
    });

    describe.skip("POST /api/copilot/stream — timeout + abort (COPILOT-11, Wave 0 stub, plan 02-02)", () => {
      it("aborts the upstream OpenRouter fetch after 30s and emits data: [TIMEOUT] (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });

      it("aborts the upstream fetch when the client closes the connection (req.signal) (Wave 0 stub)", () => {
        expect(true).toBe(true);
      });
    });
    ```

    NO import statement from `../stream/route` — production module is the 501 stub in this same wave.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/copilot/__tests__/stream.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `src/app/api/copilot/__tests__/stream.test.ts`
    - Contains 5 distinct `describe.skip(` strings (verify: `grep -c "describe.skip(" src/app/api/copilot/__tests__/stream.test.ts` returns 5)
    - Contains at least 12 `it(` cases total (verify: `grep -c "  it(" src/app/api/copilot/__tests__/stream.test.ts` returns ≥ 12)
    - Contains literal phrase `X-Demo-Mode` (forward-defining D4 contract)
    - Contains literal phrase `text/event-stream` (forward-defining the SSE shape)
    - Contains literal phrase `[TIMEOUT]` (forward-defining D7)
    - No import from `../stream/route` (production module is the 501 stub)
    - `npx vitest run src/app/api/copilot/__tests__/stream.test.ts` reports "skipped" without errors
  </acceptance_criteria>
  <done>Test file present, 5 stub describe blocks, ≥12 stub cases, vitest reports skip without error.</done>
</task>

<task type="auto">
  <name>Task 0.3: Create POST /api/copilot/stream route stub (COPILOT-09 build-gate)</name>
  <files>src/app/api/copilot/stream/route.ts</files>
  <read_first>
    - src/app/api/copilot/rag-health/route.ts (sibling pattern — 5 lines header import + GET signature)
    - .planning/phases/02-streaming-api-rag-grounding/02-CONTEXT.md (Specifics — POST only, GET 405)
  </read_first>
  <action>
    Create directory `src/app/api/copilot/stream/` if it does not exist.

    Create `src/app/api/copilot/stream/route.ts`. Exact file body:

    ```typescript
    import { NextResponse } from "next/server";

    /**
     * POST /api/copilot/stream — Wave 0 stub (plan 02-00).
     *
     * Real implementation lands in plan 02-02 (auth + rate-limit + demo short-circuit
     * + OpenRouter SSE pass-through + 30s timeout + abort propagation).
     *
     * This stub exists so `npx tsc --noEmit` has a typed entry point to sample
     * during Wave 1 (system-prompt extension) without 404 noise.
     */
    export async function POST(): Promise<NextResponse> {
      return NextResponse.json(
        { error: "Not implemented yet" },
        { status: 501 },
      );
    }

    /** GET intentionally returns 405 per CONTEXT.md Specifics (POST only). */
    export async function GET(): Promise<NextResponse> {
      return NextResponse.json(
        { error: "Method not allowed" },
        { status: 405 },
      );
    }
    ```

    No imports beyond `next/server`. No coupling to `retrieveHybrid` / `buildSystemPrompt` yet — that's plan 02-02's job.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/api/copilot/stream/route.ts` exists
    - Exports an async `POST` handler (verify: `grep -c "export async function POST" src/app/api/copilot/stream/route.ts` returns 1)
    - Exports an async `GET` handler returning 405 (verify: `grep -c "export async function GET" src/app/api/copilot/stream/route.ts` returns 1)
    - The POST body literally references status 501 (verify: `grep -c "501" src/app/api/copilot/stream/route.ts` returns ≥ 1)
    - The GET body literally references status 405 (verify: `grep -c "405" src/app/api/copilot/stream/route.ts` returns ≥ 1)
    - Does NOT import `retrieveHybrid`, `buildSystemPrompt`, `requireAuth`, or `checkRateLimit` (verify: `grep -E "retrieveHybrid|buildSystemPrompt|requireAuth|checkRateLimit" src/app/api/copilot/stream/route.ts` returns 0 matches)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Stub route exists, POST returns 501, GET returns 405, tsc green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/server/coach-rag/__tests__/system-prompt.test.ts src/app/api/copilot/__tests__/stream.test.ts` reports all skipped (no errors)
- `npx tsc --noEmit` exits 0
- `npx next lint` exits 0
- `grep -rn "describe.skip" src/lib/server/coach-rag/__tests__/ src/app/api/copilot/__tests__/` returns ≥ 9 lines (4 + 5)
- `test -f src/app/api/copilot/stream/route.ts` exits 0
</verification>

<success_criteria>
1. Two Wave 0 test stub files created in the right directories (`coach-rag/__tests__/` and `api/copilot/__tests__/`)
2. POST stream route stub returning 501 + GET stub returning 405 exists
3. All test files contain only `describe.skip` blocks — no production imports yet
4. `npx tsc --noEmit` and `npx vitest run` both green before any Wave 1 work starts
5. Nyquist sampling rate now exceeds Wave 1 implementation pace
</success_criteria>

<output>
After completion, create `.planning/phases/02-streaming-api-rag-grounding/02-00-SUMMARY.md` documenting:
- Which 3 files were created and their stub content shape (no imports, only describe.skip)
- Why the 501 POST + 405 GET stub is sufficient for Wave 1 sampling
- The exact Wave 0 → Wave 1 hand-off: plan 02-01 (system-prompt) flips the 4 prompt describe.skip blocks, plan 02-02 (stream route) flips the 5 stream describe.skip blocks
</output>
