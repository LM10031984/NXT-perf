---
phase: 08-coach-brain-method-system-prompt
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-00"
files_modified:
  - scripts/benchmark-llm.ts
  - docs/llm-benchmark.md
  - .env.local.example
autonomous: true
requirements:
  - RAG2-02
  - RAG2-03
  - RAG2-04
must_haves:
  truths:
    - "npx tsx scripts/benchmark-llm.ts --dry-run affiche les 4 modèles et 3 prompts sans appel LLM"
    - "docs/llm-benchmark.md existe avec la méthodologie, les 4 modèles, les critères et une recommandation de modèle par défaut basée sur la tarification publique"
    - "COACH_RAG_DEFAULT_MODEL est documenté dans .env.local.example avec valeur recommandée et lien vers le benchmark"
    - "L'implémentation réelle de runSingleBenchmark() permet à Laurent de lancer npm run benchmark après avoir configuré OPENROUTER_API_KEY"
  artifacts:
    - path: "scripts/benchmark-llm.ts"
      provides: "CLI benchmark fonctionnel (appels LLM réels + écriture docs/llm-benchmark.md)"
      exports:
        - "runSingleBenchmark"
        - "runAllBenchmarks"
        - "generateBenchmarkDoc"
        - "BENCHMARK_PROMPTS"
        - "MODELS_TO_BENCHMARK"
    - path: "docs/llm-benchmark.md"
      provides: "Documentation benchmark pré-remplie (méthodologie + tarifs + recommandation)"
      contains: "COACH_RAG_DEFAULT_MODEL"
    - path: ".env.local.example"
      provides: "Variable COACH_RAG_DEFAULT_MODEL documentée et décommentée"
      contains: "COACH_RAG_DEFAULT_MODEL"
  key_links:
    - from: "scripts/benchmark-llm.ts"
      to: "https://openrouter.ai/api/v1/chat/completions"
      via: "fetch avec Authorization Bearer + model param"
      pattern: "OPENROUTER_URL"
    - from: "docs/llm-benchmark.md"
      to: ".env.local.example"
      via: "COACH_RAG_DEFAULT_MODEL recommandation"
      pattern: "COACH_RAG_DEFAULT_MODEL"
---

<objective>
Wave 2 — Benchmark infrastructure RAG2-02/03/04.

Purpose: Implémenter le CLI de benchmark LLM (stub → fonctionnel), préremplir docs/llm-benchmark.md avec la méthodologie et les tarifs publics, et documenter COACH_RAG_DEFAULT_MODEL dans .env.local.example. Laurent lance `npm run benchmark` une fois OPENROUTER_API_KEY configuré pour remplir les scores qualitatifs réels.

Output:
- `scripts/benchmark-llm.ts` — CLI complet avec appels OpenRouter streaming réels
- `docs/llm-benchmark.md` — template prérempli (méthodologie + tarifs + recommendation_basée_sur_tarifs)
- `.env.local.example` — `COACH_RAG_DEFAULT_MODEL` décommenté avec valeur recommandée

Note : Le benchmark QUALITATIF (notes 1-10 de Laurent) ne peut pas être automatisé — le script crée l'infrastructure et documente le cadre, Laurent complète les scores après la vraie exécution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-coach-brain-method-system-prompt/08-CONTEXT.md
@scripts/benchmark-llm.ts
@.env.local.example
@.planning/phases/08-coach-brain-method-system-prompt/08-00-SUMMARY.md

<interfaces>
<!-- Interfaces depuis le squelette créé en Plan 08-00 -->

Types déjà définis dans scripts/benchmark-llm.ts (à implémenter) :
```typescript
interface BenchmarkPrompt { id: string; label: string; userMessage: string; }

interface ModelResult {
  model: string;
  promptId: string;
  firstTokenMs: number;
  totalMs: number;
  inputTokensEstimate: number;
  outputTokensEstimate: number;
  costEurEstimate: number;
  response: string;
  error?: string;
}

// Fonctions à implémenter (stubs dans 08-00) :
export async function runSingleBenchmark(model, prompt, systemPrompt, apiKey): Promise<ModelResult>
export async function runAllBenchmarks(systemPrompt): Promise<ModelResult[]>
export function generateBenchmarkDoc(results: ModelResult[]): string
```

Format API OpenRouter (identique à route.ts) :
```
POST https://openrouter.ai/api/v1/chat/completions
Authorization: Bearer $OPENROUTER_API_KEY
body: { model, messages, stream: true, max_tokens: 800, temperature: 0.4 }
```

Tarification publique connue ($/M tokens, source openrouter.ai, 2026-05-26) :
- anthropic/claude-haiku-4-5   : $0.80 input / $4.00 output
- openai/gpt-4o-mini            : $0.15 input / $0.60 output
- google/gemini-flash-1.5       : $0.075 input / $0.30 output
- mistralai/mistral-small-latest: $0.20 input / $0.60 output

(À vérifier par Laurent sur https://openrouter.ai/models avant le run réel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Implémenter scripts/benchmark-llm.ts (runSingleBenchmark + runAllBenchmarks + generateBenchmarkDoc)</name>
  <files>scripts/benchmark-llm.ts</files>
  <action>
Remplacer les 3 stubs `throw new Error("NOT_IMPLEMENTED")` par des implémentations réelles. Conserver tous les types et constantes définis en Plan 08-00.

**`runSingleBenchmark`** — implémentation :
```typescript
export async function runSingleBenchmark(
  model: string,
  prompt: BenchmarkPrompt,
  systemPrompt: string,
  apiKey: string,
): Promise<ModelResult> {
  const OPENROUTER_URL = "https://openrouter.ai/api/v1/chat/completions";
  const t0 = Date.now();
  let firstTokenMs = -1;
  let response = "";

  const res = await fetch(OPENROUTER_URL, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": `Bearer ${apiKey}`,
      "HTTP-Referer": "https://nxt-perf.vercel.app",
      "X-Title": "NXT Performance Benchmark",
    },
    body: JSON.stringify({
      model,
      messages: [
        { role: "system", content: systemPrompt },
        { role: "user", content: prompt.userMessage },
      ],
      stream: true,
      max_tokens: 800,
      temperature: 0.4,
    }),
  });

  if (!res.ok || !res.body) {
    const errText = await res.text().catch(() => "");
    return {
      model, promptId: prompt.id,
      firstTokenMs: -1, totalMs: Date.now() - t0,
      inputTokensEstimate: 0, outputTokensEstimate: 0,
      costEurEstimate: 0, response: "",
      error: `HTTP ${res.status}: ${errText.slice(0, 200)}`,
    };
  }

  // Lire le stream SSE ligne par ligne
  const decoder = new TextDecoder();
  const reader = res.body.getReader();
  let buf = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    buf += decoder.decode(value, { stream: true });
    let nl = buf.indexOf("\n");
    while (nl !== -1) {
      const line = buf.slice(0, nl).trim();
      buf = buf.slice(nl + 1);
      if (line.startsWith("data:")) {
        const payload = line.slice(5).trim();
        if (payload === "[DONE]") break;
        try {
          const obj = JSON.parse(payload) as { choices?: Array<{ delta?: { content?: string } }> };
          const delta = obj.choices?.[0]?.delta?.content ?? "";
          if (delta.length > 0) {
            if (firstTokenMs === -1) firstTokenMs = Date.now() - t0;
            response += delta;
          }
        } catch { /* skip malformed */ }
      }
      nl = buf.indexOf("\n");
    }
  }

  const totalMs = Date.now() - t0;
  // Estimation tokens : ~4 chars/token pour le FR
  const inputChars = systemPrompt.length + prompt.userMessage.length;
  const outputChars = response.length;
  const inputTokensEstimate = Math.ceil(inputChars / 4);
  const outputTokensEstimate = Math.ceil(outputChars / 4);
  const pricing = MODEL_PRICING[model] ?? { inputPer1M: 0, outputPer1M: 0 };
  // Coût en EUR (approximation : 1 USD ≈ 0.92 EUR, 2026)
  const costUsd = (inputTokensEstimate / 1_000_000) * pricing.inputPer1M
                + (outputTokensEstimate / 1_000_000) * pricing.outputPer1M;
  const costEurEstimate = costUsd * 0.92;

  return {
    model, promptId: prompt.id,
    firstTokenMs: firstTokenMs >= 0 ? firstTokenMs : totalMs,
    totalMs, inputTokensEstimate, outputTokensEstimate,
    costEurEstimate, response,
  };
}
```

**`runAllBenchmarks`** — appelle `runSingleBenchmark` pour chaque combinaison (4 modèles × 3 prompts) avec délai 500ms entre les appels pour éviter le rate-limiting. Lit `OPENROUTER_API_KEY` depuis `process.env`. Logue la progression.

**`generateBenchmarkDoc`** — génère un Markdown avec :
- Date du benchmark
- Tableau récapitulatif (modèle | prompt | first-token ms | total ms | coût EUR estimé | début réponse)
- Section "Notes qualitatives" avec des `[ ]` vides pour que Laurent remplisse ses scores 1-10
- Recommandation auto basée sur le coût le plus bas parmi les modèles sans erreur

**Mise à jour de `main()`** — ajouter support flag `--dry-run` :
```typescript
const isDryRun = process.argv.includes("--dry-run");
if (isDryRun) {
  console.log("DRY RUN — aucun appel LLM. Modèles et prompts configurés :");
  MODELS_TO_BENCHMARK.forEach((m) => console.log("  •", m));
  BENCHMARK_PROMPTS.forEach((p) => console.log("  •", p.id, ":", p.label));
  process.exit(0);
}
// Sinon : run réel
const apiKey = process.env.OPENROUTER_API_KEY;
if (!apiKey) {
  console.error("OPENROUTER_API_KEY non défini. Configurer dans .env.local puis :");
  console.error("  OPENROUTER_API_KEY=sk-or-... npx tsx scripts/benchmark-llm.ts");
  process.exit(1);
}
```

Contraintes :
- Pas de nouveau package (`node-fetch` interdit — utiliser `fetch` global Node 20)
- Pas de `// @ts-ignore`
- Caractères UTF-8 réels dans les logs (é, è, à, etc.)
  </action>
  <verify>
    <automated>npx tsx scripts/benchmark-llm.ts --dry-run 2>&1 | grep -E "(DRY RUN|anthropic|gpt-4o|gemini|mistral)" | head -8</automated>
  </verify>
  <done>`npx tsx scripts/benchmark-llm.ts --dry-run` affiche "DRY RUN" puis les 4 modèles et 3 prompts, sans erreur. `npx tsc --noEmit` passe sur ce fichier.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Créer docs/llm-benchmark.md + mettre à jour .env.local.example (RAG2-02/03/04)</name>
  <files>docs/llm-benchmark.md, .env.local.example</files>
  <action>
**A. Créer `docs/llm-benchmark.md`** — template de benchmark prérempli (à compléter par Laurent après le run réel).

Contenu :

```markdown
# Benchmark LLM — NXT Performance Copilote

**Date :** À remplir après le run (`npx tsx scripts/benchmark-llm.ts`)
**Objectif :** Choisir le modèle par défaut pour `COACH_RAG_DEFAULT_MODEL` — meilleur rapport qualité/coût pour le coaching immobilier FR.

## Méthodologie

- **Modèles testés :** 4 (voir tableau ci-dessous)
- **Prompts :** 3 prompts standards (`p1-mandats`, `p2-prospection`, `p3-motivation`)
- **Critères mesurés :**
  1. Qualité coaching FR (note 1-10, manuelle, par Laurent)
  2. Coût estimé par appel (input + output tokens × tarif public OpenRouter)
  3. Latence first-token (ms, mesurée par le script)
  4. Latence totale (ms, mesurée par le script)
- **Script :** `npx tsx scripts/benchmark-llm.ts`

## Tarification publique de référence

Source : https://openrouter.ai/models — consulter avant le run pour vérifier les prix actuels.

| Modèle | Input $/M tokens | Output $/M tokens |
|--------|-----------------|-------------------|
| anthropic/claude-haiku-4-5    | $0.80  | $4.00  |
| openai/gpt-4o-mini            | $0.15  | $0.60  |
| google/gemini-flash-1.5       | $0.075 | $0.30  |
| mistralai/mistral-small-latest| $0.20  | $0.60  |

## Résultats — À remplir après le run

> Lancer : `OPENROUTER_API_KEY=sk-or-... npx tsx scripts/benchmark-llm.ts`
> Le script génère les lignes ci-dessous automatiquement.

### Métriques objectifs (auto-générées)

| Modèle | Prompt | First token (ms) | Total (ms) | Coût EUR est. |
|--------|--------|-----------------|------------|---------------|
| *à remplir* | | | | |

### Qualité coaching FR — Notes subjectives (Laurent)

Instructions : noter 1-10 sur les critères suivants pour chaque réponse générée.
- **Pertinence méthode** : la réponse utilise-t-elle le vocabulaire et la structure NXT ?
- **Actionnabilité** : contient-elle au moins 1 action concrète ?
- **Ton** : tutoiement, pragmatique, pas de bullshit motivationnel ?
- **Longueur** : adaptée (ni trop courte ni trop verbeuse) ?

| Modèle | p1-mandats | p2-prospection | p3-motivation | Moyenne |
|--------|-----------|----------------|--------------|---------|
| anthropic/claude-haiku-4-5    | [ /10] | [ /10] | [ /10] | — |
| openai/gpt-4o-mini            | [ /10] | [ /10] | [ /10] | — |
| google/gemini-flash-1.5       | [ /10] | [ /10] | [ /10] | — |
| mistralai/mistral-small-latest| [ /10] | [ /10] | [ /10] | — |

## Recommandation provisoire (basée sur tarifs publics uniquement)

Sans run réel, la recommandation économique par défaut est **`google/gemini-flash-1.5`** ($0.075/$0.30 par M tokens — le moins cher parmi les 4). À confirmer ou infirmer par les notes de qualité après le run.

Si la qualité coaching FR est insuffisante (< 6/10 en moyenne), passer à **`openai/gpt-4o-mini`** (~2.5× plus cher mais bien documenté pour le FR).

## Modèle sélectionné

> À remplir après le run et notation manuelle.

```
COACH_RAG_DEFAULT_MODEL=google/gemini-flash-1.5  # à confirmer
```

Mettre à jour `.env.local.example` avec la valeur finale.

## Réponses-avec vs Réponses-sans méthode coaching injectée

(METHOD-03 — vérification qualitative manuelle)

Tester les 3 mêmes prompts avec `coachingMethod: undefined` vs `coachingMethod: [contenu réel]` en appelant directement `/api/copilot/stream` depuis le browser ou Postman.

Observation attendue : avec méthode injectée, les réponses doivent :
- Utiliser des questions ouvertes (formulations NXT signature)
- Respecter le ratio 65% écoute / 35% conseil
- Structurer selon les 7 règles d'or
```

**B. Mettre à jour `.env.local.example`** — décommenter et mettre à jour la ligne `COACH_RAG_DEFAULT_MODEL` :

Remplacer :
```bash
# COACH_RAG_DEFAULT_MODEL=anthropic/claude-sonnet-4-5
```

Par :
```bash
# Modèle copilote par défaut (utilisé par /api/copilot/stream)
# Choisi après benchmark — voir docs/llm-benchmark.md
# Recommandation provisoire (tarifs publics) : google/gemini-flash-1.5
# Valider avec : npx tsx scripts/benchmark-llm.ts
COACH_RAG_DEFAULT_MODEL=google/gemini-flash-1.5
```

Note : la valeur `google/gemini-flash-1.5` est la recommandation par défaut basée sur les tarifs publics (le moins cher). Laurent peut changer après le benchmark qualitatif.
  </action>
  <verify>
    <automated>grep -q "COACH_RAG_DEFAULT_MODEL=google/gemini-flash-1.5" /Users/laurentmarx/Documents/Dashboard/NXT-perf/.env.local.example && grep -q "llm-benchmark" /Users/laurentmarx/Documents/Dashboard/NXT-perf/.env.local.example && test -f /Users/laurentmarx/Documents/Dashboard/NXT-perf/docs/llm-benchmark.md && echo OK</automated>
  </verify>
  <done>`docs/llm-benchmark.md` existe avec la méthodologie, les 4 modèles, les tableaux de notes vides, et la recommandation provisoire. `.env.local.example` contient `COACH_RAG_DEFAULT_MODEL=google/gemini-flash-1.5` avec commentaire de doc. (RAG2-02, RAG2-03, RAG2-04)</done>
</task>

</tasks>

<verification>
Checks finaux après complétion du plan :

1. `npx tsx scripts/benchmark-llm.ts --dry-run` — affiche DRY RUN + 4 modèles + 3 prompts, exit 0
2. `npx tsc --noEmit` — aucune erreur TypeScript
3. `test -f docs/llm-benchmark.md` — fichier présent
4. `grep "COACH_RAG_DEFAULT_MODEL" .env.local.example` — ligne décommentée avec valeur recommandée
5. `grep "llm-benchmark.md" .env.local.example` — référence au doc dans le commentaire

Vérification manuelle optionnelle (Laurent) :
- Lancer `OPENROUTER_API_KEY=sk-or-... npx tsx scripts/benchmark-llm.ts` avec une vraie clé
- Comparer les réponses des 4 modèles sur les 3 prompts
- Remplir les notes dans `docs/llm-benchmark.md`
- Mettre à jour `COACH_RAG_DEFAULT_MODEL` dans `.env.local.example` si besoin
</verification>

<success_criteria>
- RAG2-02 : `docs/llm-benchmark.md` documente la méthodologie + 4 modèles + critères + tableaux
- RAG2-03 : `COACH_RAG_DEFAULT_MODEL` dans `.env.local.example` avec valeur recommandée
- RAG2-04 : env var reste configurable sans redéploiement (lecture via `process.env.COACH_RAG_DEFAULT_MODEL` déjà en place dans route.ts ligne 35)
- `npx tsx scripts/benchmark-llm.ts --dry-run` fonctionne
- `npx tsc --noEmit` clean
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-coach-brain-method-system-prompt/08-02-SUMMARY.md`
</output>
