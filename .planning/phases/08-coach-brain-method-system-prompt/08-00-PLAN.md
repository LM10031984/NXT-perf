---
phase: 08-coach-brain-method-system-prompt
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/coaching-method/coaching-method.md
  - src/lib/server/coach-rag/__tests__/coaching-method-injection.test.ts
  - scripts/benchmark-llm.ts
autonomous: true
requirements:
  - METHOD-01
  - METHOD-03
  - RAG2-02
must_haves:
  truths:
    - "src/data/coaching-method/coaching-method.md existe avec un frontmatter de versioning (version, extractedFrom, extractedAt)"
    - "coaching-method-injection.test.ts contient un squelette importable — les tests sont FAILING (rouge TDD)"
    - "scripts/benchmark-llm.ts contient le squelette CLI (imports, signatures, main() stub) — compilable sans erreur"
  artifacts:
    - path: "src/data/coaching-method/coaching-method.md"
      provides: "Source canonique versionnée de la méthode coaching NXT"
      contains: "version:"
    - path: "src/lib/server/coach-rag/__tests__/coaching-method-injection.test.ts"
      provides: "Tests rouge pour METHOD-03 (injection <coaching-method>)"
    - path: "scripts/benchmark-llm.ts"
      provides: "Squelette du benchmark CLI RAG2-02"
  key_links:
    - from: "src/data/coaching-method/coaching-method.md"
      to: "src/lib/server/coach-rag/coaching-method.ts"
      via: "contenu à synchroniser (Plan 08-01)"
      pattern: "extractedFrom"
---

<objective>
Wave 0 — Stubs et artefacts sources avant toute modification.

Purpose: Créer les 3 artefacts sans lesquels les plans 08-01 et 08-02 ne peuvent pas compiler ni tester. Aucune logique métier ici — uniquement les contrats et squelettes.

Output:
- `src/data/coaching-method/coaching-method.md` — copie versionnée de la méthode (source de vérité textuelle)
- `coaching-method-injection.test.ts` — tests FAILING décrivant le comportement attendu de `buildSystemPrompt` avec `coachingMethod`
- `scripts/benchmark-llm.ts` — CLI stub compilable, pas encore fonctionnel
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-coach-brain-method-system-prompt/08-CONTEXT.md
@src/lib/server/coach-rag/coaching-method.ts
@src/lib/server/coach-rag/system-prompt.ts
@src/lib/server/coach-rag/__tests__/system-prompt.test.ts
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Créer src/data/coaching-method/coaching-method.md versionné</name>
  <files>src/data/coaching-method/coaching-method.md</files>
  <action>
Créer le dossier `src/data/coaching-method/` s'il n'existe pas, puis créer `coaching-method.md`.

Contenu : copier le texte Markdown du contenu de la constante `COACHING_METHOD_NXT` depuis `src/lib/server/coach-rag/coaching-method.ts` (sans les backticks ni les guillemets TypeScript). Ajouter **en en-tête** (avant le premier `#`) un bloc frontmatter YAML de versioning (per D-01 de 08-CONTEXT.md) :

```
---
version: "1.0"
extractedFrom: "nxt-coach/data/coaching-method.md"
extractedAt: "2026-05-26"
notes: "Extrait de 24 sessions valides (ratio 71% Q ouvertes). Mise à jour : régénérer COACHING_METHOD_NXT dans coaching-method.ts."
---
```

Puis coller le contenu Markdown (le `# Méthode coaching NXT` et tout ce qui suit) tel quel depuis la constante existante.

NE PAS modifier `coaching-method.ts` dans cette tâche — c'est le rôle du Plan 08-01.
  </action>
  <verify>
    <automated>grep -q 'version: "1.0"' /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/data/coaching-method/coaching-method.md && grep -q 'extractedFrom' /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/data/coaching-method/coaching-method.md && grep -q '# Méthode coaching NXT' /Users/laurentmarx/Documents/Dashboard/NXT-perf/src/data/coaching-method/coaching-method.md && echo OK</automated>
  </verify>
  <done>Le fichier existe, contient le frontmatter YAML avec version/extractedFrom/extractedAt, et le contenu Markdown complet de la méthode.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : Créer coaching-method-injection.test.ts (tests FAILING — rouge TDD)</name>
  <files>src/lib/server/coach-rag/__tests__/coaching-method-injection.test.ts</files>
  <behavior>
    - Test 1 : `buildSystemPrompt` avec `coachingMethod` non vide inclut `<coaching-method>` dans le résultat
    - Test 2 : `buildSystemPrompt` avec `coachingMethod` non vide inclut `</coaching-method>` dans le résultat
    - Test 3 : le contenu de `coachingMethod` passé est présent entre les balises
    - Test 4 : `buildSystemPrompt` sans `coachingMethod` (champ absent ou `undefined`) ne contient PAS `<coaching-method>`
    - Test 5 : la section `<coaching-method>` apparaît AVANT les blocs `<rag-source>` dans le prompt (ordre D-02)
    - Test 6 : la section `<coaching-method>` apparaît APRÈS le bloc `<user-context>` dans le prompt (ordre D-02)
  </behavior>
  <action>
Créer le fichier de test en important `buildSystemPrompt` depuis `../system-prompt`.

Structure du fichier :
```typescript
import { describe, it, expect } from "vitest";
import { buildSystemPrompt } from "../system-prompt";
import type { RetrievedChunk } from "../retrieve";

const SAMPLE_METHOD = "## Règle 1\nPoser une question ouverte.";

function makeChunk(over = {}): RetrievedChunk { /* même helper que system-prompt.test.ts */ }

describe("buildSystemPrompt — METHOD-02/03 injection <coaching-method>", () => {
  it("TEST 1 : inclut <coaching-method> quand coachingMethod est fourni", ...)
  it("TEST 2 : inclut </coaching-method> quand coachingMethod est fourni", ...)
  it("TEST 3 : le contenu de coachingMethod apparaît dans le bloc", ...)
  it("TEST 4 : n'émet PAS <coaching-method> si coachingMethod est undefined", ...)
  it("TEST 5 : <coaching-method> apparaît avant <rag-source>", ...)
  it("TEST 6 : <coaching-method> apparaît après <user-context>", ...)
});
```

Ces tests DOIVENT ÉCHOUER à ce stade (l'interface `BuildSystemPromptInput` ne contient pas encore `coachingMethod`). C'est l'état rouge attendu du cycle TDD.

Utiliser des caractères UTF-8 réels (é, è, etc.) dans les labels de tests.
  </action>
  <verify>
    <automated>MISSING — Wave 0 seed : npx vitest run src/lib/server/coach-rag/__tests__/coaching-method-injection.test.ts 2>&1 | grep -E "(FAIL|Cannot find|does not provide)" | head -5</automated>
  </verify>
  <done>Le fichier existe, compile (imports valides), et les 6 tests sont en état FAILING (rouge TDD) car `coachingMethod` n'est pas encore dans `BuildSystemPromptInput`.</done>
</task>

<task type="auto">
  <name>Tâche 3 : Créer scripts/benchmark-llm.ts (squelette CLI compilable)</name>
  <files>scripts/benchmark-llm.ts</files>
  <action>
Créer `scripts/benchmark-llm.ts` — squelette du benchmark LLM (RAG2-02). Ce fichier doit être compilable par `npx tsc --noEmit` mais NE PAS faire d'appels LLM réels.

Contenu attendu :

```typescript
/**
 * scripts/benchmark-llm.ts
 *
 * Benchmark des 4 modèles OpenRouter pour le copilote NXT.
 *
 * Usage : npx tsx scripts/benchmark-llm.ts
 *
 * Modèles testés :
 *   1. anthropic/claude-haiku-4-5
 *   2. openai/gpt-4o-mini
 *   3. google/gemini-flash-1.5
 *   4. mistralai/mistral-small-latest
 *
 * Résultats écrits dans docs/llm-benchmark.md
 * (les appels LLM réels sont faits par Laurent après avoir configuré OPENROUTER_API_KEY)
 */

import * as fs from "fs";
import * as path from "path";

// --- Types ---

interface BenchmarkPrompt {
  id: string;
  label: string;
  userMessage: string;
}

interface ModelResult {
  model: string;
  promptId: string;
  firstTokenMs: number;
  totalMs: number;
  inputTokensEstimate: number;
  outputTokensEstimate: number;
  costEurEstimate: number;
  response: string;
  error?: string;
}

// --- Prompts standards (3 prompts, per D-04 de 08-CONTEXT.md) ---

export const BENCHMARK_PROMPTS: BenchmarkPrompt[] = [
  {
    id: "p1-mandats",
    label: "Ratio mandats faible",
    userMessage: "Mon ratio RDV → mandats est de 0.4, la cible c'est 2.0. Comment je m'améliore ?",
  },
  {
    id: "p2-prospection",
    label: "Plan de prospection hebdomadaire",
    userMessage: "Donne-moi un plan de prospection concret pour cette semaine. Je démarre à zéro.",
  },
  {
    id: "p3-motivation",
    label: "Agent découragé (mode soutien)",
    userMessage: "J'en ai marre, j'arrive pas à signer. Ça fait 3 semaines sans mandat.",
  },
];

// --- Modèles à benchmarker ---

export const MODELS_TO_BENCHMARK = [
  "anthropic/claude-haiku-4-5",
  "openai/gpt-4o-mini",
  "google/gemini-flash-1.5",
  "mistralai/mistral-small-latest",
] as const;

// Coûts publics approximatifs ($/M tokens) — à vérifier sur https://openrouter.ai/models
// Source : OpenRouter pricing page, consulté 2026-05-26
export const MODEL_PRICING: Record<string, { inputPer1M: number; outputPer1M: number }> = {
  "anthropic/claude-haiku-4-5":   { inputPer1M: 0.80,  outputPer1M: 4.00  },
  "openai/gpt-4o-mini":           { inputPer1M: 0.15,  outputPer1M: 0.60  },
  "google/gemini-flash-1.5":      { inputPer1M: 0.075, outputPer1M: 0.30  },
  "mistralai/mistral-small-latest": { inputPer1M: 0.20, outputPer1M: 0.60 },
};

// --- Fonctions principales (stubs — implémentation Plan 08-02) ---

/**
 * Appelle OpenRouter en mode streaming pour un modèle + prompt donné.
 * Mesure first-token latency et total latency.
 * @throws si OPENROUTER_API_KEY n'est pas défini
 */
export async function runSingleBenchmark(
  _model: string,
  _prompt: BenchmarkPrompt,
  _systemPrompt: string,
  _apiKey: string,
): Promise<ModelResult> {
  throw new Error("NOT_IMPLEMENTED — implémenté dans Plan 08-02");
}

/**
 * Lance tous les benchmarks (4 modèles × 3 prompts = 12 appels) et retourne les résultats.
 */
export async function runAllBenchmarks(_systemPrompt: string): Promise<ModelResult[]> {
  throw new Error("NOT_IMPLEMENTED — implémenté dans Plan 08-02");
}

/**
 * Génère docs/llm-benchmark.md depuis les résultats.
 */
export function generateBenchmarkDoc(_results: ModelResult[]): string {
  throw new Error("NOT_IMPLEMENTED — implémenté dans Plan 08-02");
}

// --- Point d'entrée CLI ---

async function main(): Promise<void> {
  console.log("NXT Performance — Benchmark LLM");
  console.log("Modèles :", MODELS_TO_BENCHMARK.join(", "));
  console.log("Prompts  :", BENCHMARK_PROMPTS.map((p) => p.id).join(", "));
  console.log("");
  console.log("ATTENTION : Ce script est un stub. Les appels LLM réels nécessitent");
  console.log("OPENROUTER_API_KEY configuré + npm run benchmark (Plan 08-02).");

  const outDir = path.join(process.cwd(), "docs");
  if (!fs.existsSync(outDir)) {
    fs.mkdirSync(outDir, { recursive: true });
  }

  console.log("\nSortie prévue : docs/llm-benchmark.md");
}

main().catch((err: unknown) => {
  console.error(err);
  process.exit(1);
});
```

Ce fichier doit passer `npx tsc --noEmit` sans erreur.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scripts/benchmark-llm" | head -5 || echo "TSC_OK_NO_ERRORS_IN_BENCHMARK"</automated>
  </verify>
  <done>Le fichier existe, est typé correctement (aucune erreur tsc sur ce fichier), exporte les constantes `BENCHMARK_PROMPTS`, `MODELS_TO_BENCHMARK`, `MODEL_PRICING`, et les 3 fonctions stub. `npx tsx scripts/benchmark-llm.ts` affiche le message d'info sans crash.</done>
</task>

</tasks>

<verification>
À la fin du plan :
1. `src/data/coaching-method/coaching-method.md` existe avec frontmatter `version: "1.0"` + `extractedFrom` + `# Méthode coaching NXT`
2. `coaching-method-injection.test.ts` existe, s'importe sans crash, et les tests sont FAILING (rouge TDD attendu)
3. `scripts/benchmark-llm.ts` passe `npx tsc --noEmit` sans erreur sur ce fichier
4. Aucune modification des fichiers existants (`system-prompt.ts`, `coaching-method.ts`, `route.ts`)
</verification>

<success_criteria>
- `src/data/coaching-method/coaching-method.md` présent et versionné (METHOD-01 partiellement)
- Tests de coaching-method-injection.test.ts en état rouge (prêts pour le vert en Plan 08-01)
- scripts/benchmark-llm.ts compilable — squelette des fonctions `runSingleBenchmark`, `runAllBenchmarks`, `generateBenchmarkDoc`
- `npx tsc --noEmit` passe (hors erreurs pré-existantes éventuelles)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-coach-brain-method-system-prompt/08-00-SUMMARY.md`
</output>
